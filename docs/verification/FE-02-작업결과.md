# FE-02 : 작업결과

- **TASK**: GNB 컴포넌트 구현
- **상태**: 완료(PM 승인 2026-08-04)
- **선행 TASK**: FE-01
- **근거 문서**: PRD 7-4, IA v1.2 6.1(글로벌 내비게이션), 디자인가이드 v1.0 6.1
- **변경 파일**: `frontend/src/components/gnb.tsx`(신규), `frontend/src/lib/nav-items.ts`(신규), `frontend/src/app/layout.tsx`(HeaderShell→Gnb 교체), `frontend/src/components/header-shell.tsx`·관련 테스트 삭제, `frontend/e2e/gnb.spec.ts`(신규), `frontend/playwright.config.ts`(신규), `frontend/package.json`(Playwright 추가)

## 결과 요약

- **4개 진입점**(IA v1.2 6.1): 티어리스트(`/`), 아이템 빌드(`/items/builds`), 증강체 정보(`/augments`), 전적 분석(`/analysis`). 조합 상세·`/kpi`는 GNB에 노출하지 않음(IA 문서 규칙 그대로 반영)
- **데스크톱/태블릿**: 좌측 로고 + 가로 메뉴 + (우측 패치 배지는 이번 TASK 범위 밖 — 아래 "범위 밖" 참고). 현재 페이지 메뉴는 `font-bold text-text-primary`(#1A1A1A), 비활성은 `text-text-secondary`(#666666) — 디자인가이드 6.1 규칙 그대로
- **모바일(<768px)**: 로고 + 햄버거 버튼만 노출, 탭하면 `role="dialog" aria-modal="true"` 전체화면 드로어가 열리고 동일 4개 링크를 세로로 보여줌. 드로어 닫기 버튼과, 메뉴 항목 클릭 시에도 드로어가 닫힘
- 태블릿과 데스크톱은 디자인 스펙상 동일 구조라 Tailwind 기본 `md:` 브레이크포인트 하나로 3단계 반응형을 표현(WBS "3개 브레이크포인트" 요구사항 충족)
- FE-01의 임시 `HeaderShell`을 완전히 대체, 관련 파일 삭제

## GNB 로고 텍스트 확인 (PM 결정)

디자인가이드 6.1·화면설계서 모바일 와이어프레임은 로고 텍스트를 "TFT Meta"로 명시하지만, PM 확인 결과 프로젝트명 그대로 **"TFT Hideout"**을 유지하기로 결정(구버전 플레이스홀더로 판단). `/docs/reference/design-tokens.md`에 결정 사항 기록해 다음 세션이 재질문하지 않도록 함.

## 범위 밖으로 판단해 이번 TASK에 포함하지 않은 것

- **우측 "현재 패치 배지"**(디자인가이드 6.1): WBS `TASK 설명`("데스크톱/태블릿 가로바, 모바일 햄버거+드로어, 4개 진입점")과 `테스트 요구사항`에는 언급이 없고, 실제 패치 데이터를 보여주려면 프론트엔드 최초의 백엔드 API 연동(base URL 환경변수, 클라이언트 fetch 유틸)이 필요해 이번 스캐폴딩성 컴포넌트 TASK 범위를 벗어난다고 판단했습니다. 실제 데이터 대신 가짜 값을 하드코딩하고 싶지 않아 배지 자체를 아직 넣지 않았습니다. **API-06(`GET /catalog/patches/current`)과 연동하는 시점(FE-03 등 첫 카탈로그 페이지, 또는 필요 시 이 TASK를 재오픈)에 추가할지 PM 확인 부탁드립니다.**

## 자체 검증

- `npm run lint` / `npm run format:check` 통과
- `npm test`(Vitest+RTL) 19/19 통과 — 브레이크포인트 클래스(`hidden md:flex`, `md:hidden`), 4개 진입점 텍스트, 활성/비활성 스타일, 드로어 열림/닫힘(버튼·링크 클릭) 검증
- `npm run test:e2e`(Playwright, Pixel 7 모바일 뷰포트) 1/1 통과 — 햄버거 클릭 → 드로어 노출 → 닫기 → 드로어 사라짐 스모크
- `npm run build`(정적 export) 성공
- `npm run dev` + Playwright 스크린샷으로 데스크톱(1280px)·태블릿(900px)·모바일(375px, 드로어 닫힘/열림) 4장 육안 확인, 콘솔 에러 없음

## 다음 세션을 위한 메모

- **패치 배지**: 위 "범위 밖" 참고. 추가 시점 PM 확인 필요.
- **프론트엔드 API 연동 방식 미정**: 지금까지 어떤 FE-* TASK도 백엔드(Render, `https://tft-hideout-backend.onrender.com`) 호출을 구현한 적이 없다. FE-01에서 `output: "export"`(정적 export)로 확정했기 때문에 서버 사이드 렌더링이 없고, 모든 데이터 fetching은 클라이언트 사이드(useEffect/fetch 또는 SWR류)여야 한다. 그런데 WBS FE-03 TASK 설명에는 "ISR 렌더링"이라고 적혀 있어 static export 결정과 모순된다 — FE-03 착수 전에 PM과 이 모순을 먼저 정리해야 한다(ISR을 쓰려면 Cloudflare에 서버 런타임이 있는 배포 방식으로 SET-07 결정을 재검토해야 하고, 정적 export를 유지하려면 WBS 문구를 클라이언트 fetch로 수정해야 함).
- Playwright e2e는 이번 TASK 자체 검증용으로만 로컬 실행했고 CI(`ci.yml`)에는 아직 연결하지 않았다(TEST-08이 전체 E2E CI 파이프라인을 구성할 예정, CLAUDE.md 10.2). 필요하면 그 전에라도 CI에 추가할지 PM 판단 필요.
