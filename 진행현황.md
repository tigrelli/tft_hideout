# 진행현황.md — TFT Hideout 개발 진행 현황

이 문서는 `TFT_Hideout_WBS.xlsx`의 97개 TASK를 기준으로 실제 개발 진행 상태를 추적하는 **살아있는 문서**다. 
클로드 코드와 PM은 새 기능을 개발할 때 WBS 원본이 아니라 이 문서를 기준으로 다음 작업을 정하고, 완료 여부를 갱신한다.


> **갱신 규칙 (CLAUDE.md 3장과 동일)**: 클로드 코드가 TASK를 구현 → 단위 테스트 작성·통과 → 자체 검증 → PM에게 확인 요청 → **PM이 승인한 뒤에만** 아래 표의 상태를 `완료`로 변경하고 PM확인/커밋 컬럼을 채운다. PM 확인 전에는 상태를 `진행중`으로 유지하고, git 커밋·push도 하지 않는다.


> **테스트 요구사항 컬럼**: 각 TASK별 구체적인 단위/통합 테스트 항목은 `TFT_Hideout_WBS.xlsx`의 `테스트 요구사항` 컬럼(K열)을 참고한다. 여기서는 상태 추적에 집중하기 위해 생략했다.


## 진행률 요약


- 전체 TASK 수: **100개**(SET-16·DATA-16·FE-13·FE-14·CHAT-11 신설, SET-05·SET-11·SET-12 취소 3개 포함, 활성 97개)

- 완료: 58개 / 진행중: 1개(SET-16, Riot API 키 재발급 대기) / PM확인대기: 0개 / 보류: 1개(KPI-03, 이력 저장·알림 채널 설계 미확정) / 대기: 37개 / 취소: 3개(SET-05, v1.7 Redis 제거로 폐기 / SET-11, Metabase 무료 플랜 512MB OOM으로 자체 구현 전환 / SET-12, Figma MCP 미사용 결정으로 폐기) (자동 갱신 아님 — TASK 상태 변경 시 이 요약도 함께 수기 갱신할 것)


### 대그룹별 TASK 수


| 대그룹 | 하위영역 | TASK 수 |
|---|---|---|
| 시스템설정 | - | 16 |
| 코딩 | 데이터파이프라인 | 16 |
| 코딩 | 백엔드API | 14 |
| 코딩 | 챗봇RAG | 11 |
| 코딩 | 사후패인분석 | 10 |
| 코딩 | 프론트엔드 | 14 |
| 코딩 | KPI모니터링 | 3 |
| 테스트 | - | 11 |
| 배포릴리즈 | - | 5 |
| **합계** | | **100** |

\* 시스템설정 16개 중 SET-05(Upstash Redis 생성)는 2026-07-31 PM 결정, SET-11(Metabase 배포)은 2026-08-03 PM 결정, SET-12(Figma 연동)는 2026-08-04 PM 결정으로 각각 취소됨(활성 TASK는 97개). 코드는 결번으로 유지하며 위 집계에는 포함. SET-16(Riot Personal Key)은 2026-08-04 SET-10에서 분리 신설(아래 이력 참고). 프론트엔드 14개는 FE-12(KPI 대시보드 페이지, SET-11 취소에 따른 대체)·FE-13(티어리스트 카드 챔피언 이미지 표시, 2026-08-05 PM 요청으로 신설)·FE-14(조합 상세 헥스 배치도 실좌표 반영, 2026-08-06 PM 요청으로 신설) 신설분 포함. 데이터파이프라인 16개는 DATA-16(증강체 설명 템플릿 플레이스홀더 정리, FE-06 실데이터 검증 중 발견해 2026-08-05 PM 결정으로 신설) 포함. 챗봇RAG 11개는 CHAT-11(후속질문 동적 생성·LLM 기반, FE-09 검증 중 논의해 2026-08-06 PM 결정으로 신설) 포함. (2026-08-06: 이 표의 프론트엔드 행이 FE-14 신설 이후 13으로 남아있던 오기를 실제 TASK 수(14, FE-01~FE-14)에 맞춰 함께 바로잡음 — 합계도 이에 따라 갱신)

---


## 범례


- **상태**: 대기 / 진행중 / PM확인대기 / 완료 / 취소(범위 변경으로 폐기, 코드는 결번 유지)

- **PM확인**: 승인한 PM 확인 결과(승인일자 또는 `-`)

- **커밋/Push**: 완료 후 실제 커밋된 커밋 해시 또는 PR 번호(`-`는 미진행)

- **우선순위**: 필수 = 1.0 범위에 반드시 포함 / 권장 = 품질을 높이나 지연 가능


## A. 시스템 설정 (SET)


| WBS코드 | IA/화면 매칭 | TASK명 | 우선순위 | 선행TASK | 상태 | PM확인 | 커밋/Push |
|---|---|---|---|---|---|---|---|
| **SET-01** | - | GitHub 저장소 생성 및 브랜치 전략 수립 | 필수 | - | 완료 | 2026-07-31 | 686b279 · `docs/verification/SET-01-작업결과.md` |
| **SET-02** | - | 로컬 개발 환경 및 필수 프로그램 설치 | 필수 | SET-01 | 완료 | 2026-07-31 | 4b16b38 · `docs/verification/SET-02-작업결과.md` |
| **SET-03** | - | 리포지토리 초기 구조 스캐폴딩 | 필수 | SET-01 | 완료 | 2026-07-31 | 0bccf96 · `docs/verification/SET-03-작업결과.md` |
| **SET-04** | - | Supabase 프로젝트 생성(Postgres+pgvector) | 필수 | - | 완료 | 2026-07-31 | b1d3995 · `docs/verification/SET-04-작업결과.md` |
| **SET-05** | - | (취소) Upstash Redis 생성 — v1.7 Redis 제거 결정으로 폐기 | 취소 | - | 취소 | 2026-07-31 | - |
| **SET-06** | - | Render 백엔드 배포 파이프라인 구성 | 필수 | SET-01,SET-03 | 완료 | 2026-07-31 | a58ff61(PR #1) · `docs/verification/SET-06-작업결과.md` |
| **SET-07** | - | Cloudflare Pages 프론트엔드 배포 파이프라인 구성 | 필수 | SET-01,SET-03 | 완료 | 2026-07-31 | 3fa9773(PR #3) · `docs/verification/SET-07-작업결과.md` |
| **SET-08** | - | GitHub Actions 워크플로우 스캐폴딩 | 필수 | SET-01 | 완료 | 2026-07-31 | 051e7a0(PR #4) · `docs/verification/SET-08-작업결과.md` |
| **SET-09** | - | 환경변수·시크릿 관리 체계 구성 | 필수 | SET-04,SET-06,SET-07,SET-08 | 완료 | 2026-07-31 | 2f36f93(PR #5) · `docs/verification/SET-09-작업결과.md` |
| **SET-10** | - | 외부 API 키 발급 및 검증(Groq·HF만, Riot은 SET-16으로 분리) | 필수 | - | 완료 | 2026-08-04 | 560982b(task/FE-01, PR #10, main 머지 완료) · `docs/verification/SET-10-작업결과.md` |
| **SET-16** | - | Riot Personal Key 발급 및 검증 | 필수 | - | 진행중(Riot API 키 재발급 대기, 기존 Development Key 401 만료) | - | - |
| **SET-11** | - | (취소) Metabase 배포(Render 2번째 서비스) — 무료 플랜 512MB OOM으로 자체 구현(KPI-01·FE-12) 전환 | 취소 | - | 취소 | 2026-08-03 | - |
| **SET-12** | - | (취소) Figma 연동 및 프론트엔드 디자인 스킬 활성화 | 취소 | - | 취소 | 2026-08-04 | - |
| **SET-13** | - | 커밋/PR/배포 승인 프로세스 정의 | 필수 | SET-01 | 완료 | 2026-08-03 | bf390c3 · `docs/verification/SET-13-작업결과.md` |
| **SET-14** | - | CI 자동 테스트 게이트 구성 | 필수 | SET-01,SET-08 | 완료 | 2026-08-03 | 4b7560b(PR #8) · `docs/verification/SET-14-작업결과.md` |
| **SET-15** | - | 근거 문서 경량 참조본 생성(/docs/reference/*.md) | 필수 | SET-03 | 완료 | 2026-08-03(소급, 파일은 2026-07-30 최초 커밋에 포함) | 686b279 · `docs/verification/SET-15-작업결과.md` |

## B-1. 코딩 > 데이터 파이프라인 (DATA)


| WBS코드 | IA/화면 매칭 | TASK명 | 우선순위 | 선행TASK | 상태 | PM확인 | 커밋/Push |
|---|---|---|---|---|---|---|---|
| **DATA-01** | 공통(전 화면) | DB 스키마 설계-정적 테이블 | 필수 | SET-04 | 완료 | 2026-08-03 | d05001b(task/DATA-01, PR #9(2026-08-04) main 머지 완료) · `docs/verification/DATA-01-작업결과.md` |
| **DATA-02** | 티어리스트·조합상세·아이템빌드 | DB 스키마 설계-메타조합 테이블 | 필수 | DATA-01 | 완료 | 2026-08-03 | 36f3f8a(task/DATA-02, PR #9(2026-08-04) main 머지 완료) · `docs/verification/DATA-02-작업결과.md` |
| **DATA-03** | 사후패인분석·분석리포트 | DB 스키마 설계-분석/로그 테이블 | 필수 | DATA-01 | 완료 | 2026-08-03 | e5fe331(task/DATA-03, PR #9(2026-08-04) main 머지 완료) · `docs/verification/DATA-03-작업결과.md` |
| **DATA-04** | 챗봇위젯 | 벡터DB 스키마 및 인덱스 구성 | 필수 | DATA-01,SET-04 | 완료 | 2026-08-03 | 62a6b82(task/DATA-04, PR #9(2026-08-04) main 머지 완료) · `docs/verification/DATA-04-작업결과.md` |
| **DATA-05** | 공통 | op.gg MCP 응답 스키마 확인 스파이크 | 필수 | - | 완료 | 2026-08-04 | 2f50348(task/FE-01, PR #10, main 머지 완료) · `docs/spike/opgg-schema.md`, `docs/verification/DATA-05-작업결과.md` |
| **DATA-06** | 공통 | TFT DDragon 신규 구조 확인 스파이크 | 필수 | - | 완료 | 2026-08-04 | 728ae6e(task/FE-01, PR #10, main 머지 완료) · `docs/spike/tft-ddragon.md`, `docs/verification/DATA-06-작업결과.md` |
| **DATA-07** | 증강체정보 | is_legend_related 판별 방법 확인 스파이크 | 필수 | DATA-05 | 완료 | 2026-08-04 | 280dc12(task/FE-01, PR #10, main 머지 완료) · `docs/spike/legend-augment.md`, `docs/verification/DATA-07-작업결과.md` |
| **DATA-08** | 공통 | op.gg MCP 배치 수집 워커 구현 | 필수 | DATA-05 | 완료 | 2026-08-04 | 895b20e(task/FE-01, PR #10, main 머지 완료) · `docs/verification/DATA-08-작업결과.md` |
| **DATA-09** | 공통 | ID-이름 매핑 갱신 로직 구현 | 필수 | DATA-06,DATA-08 | 완료 | 2026-08-04 | 7e5ac25(task/FE-01, PR #10, main 머지 완료) · `docs/verification/DATA-09-작업결과.md` |
| **DATA-10** | 공통 | 데이터 정규화 및 patch_version 태깅 구현 | 필수 | DATA-02,DATA-08 | 완료 | 2026-08-04 | 6bb00bf(task/FE-01, PR #10, main 머지 완료) · `docs/verification/DATA-10-작업결과.md` |
| **DATA-11** | 공통 | 임베딩 생성 파이프라인 구현 | 필수 | DATA-04,DATA-10,SET-10 | 완료 | 2026-08-04 | 755128b(task/FE-01, PR #10, main 머지 완료) · `docs/verification/DATA-11-작업결과.md` |
| **DATA-12** | 공통 | 자동 패치 감지 트리거 구현 | 필수 | DATA-08,SET-08 | 완료 | 2026-08-04 | 8821fdd(task/FE-01, PR #10, main 머지 완료) · `docs/verification/DATA-12-작업결과.md` |
| **DATA-13** | 공통 | 원자적 패치 전환 트랜잭션 구현 | 필수 | DATA-10,DATA-11 | 완료 | 2026-08-04 | 454ad6c(task/FE-01, PR #10, main 머지 완료) · `docs/verification/DATA-13-작업결과.md` |
| **DATA-14** | 공통 | 배치 동시 실행 방지 구현 | 필수 | DATA-12 | 완료 | 2026-08-04 | ba55f23(feat) · f5f9b49·ed3f1e3·227178c(검증 중 발견한 배포 버그 3건 수정) · schedule 활성화 커밋(1일 1회 03:00 KST) · `docs/verification/DATA-14-작업결과.md` |
| **DATA-15** | 공통 | 캐시 정리 배치 구현(Postgres, v1.7) | 필수 | DATA-13,DATA-03 | 완료 | 2026-08-04 | 06a11d2(feat) · `docs/verification/DATA-15-작업결과.md` |
| **DATA-16** | 증강체정보 | 증강체 설명 템플릿 플레이스홀더 정리 | 권장 | DATA-08 | 완료 | 2026-08-05 | 36fba5b(feat) · `docs/verification/DATA-16-작업결과.md` |

\* **⚠ DATA-09 착수 전 확인**: ID-이름 매핑은 PM 결정(2026-08-04)에 따라 Community Dragon(`raw.communitydragon.org`) 기준으로 설계한다(공식 "TFT DDragon" 분리 엔드포인트 미확인, `docs/spike/tft-ddragon.md` 참고). **Set 18 런칭(2026-08-12) 이후에는 공식 TFT DDragon 엔드포인트가 그 사이 공개됐는지 재확인**한 뒤 DATA-06 결론을 그대로 쓸지 다시 판단할 것(PM 결정, 재확인 전까지는 별도 액션 불필요).

## B-2. 코딩 > 백엔드 API (API)


| WBS코드 | IA/화면 매칭 | TASK명 | 우선순위 | 선행TASK | 상태 | PM확인 | 커밋/Push |
|---|---|---|---|---|---|---|---|
| **API-01** | 공통 | FastAPI 프로젝트 스캐폴딩 | 필수 | SET-02,SET-03 | 완료 | 2026-08-03 | 6790037(task/API-01, PR #9(2026-08-04) main 머지 완료) · `docs/verification/API-01-작업결과.md` |
| **API-02** | 티어리스트 | GET /catalog/tierlist 구현 | 필수 | DATA-02,API-01 | 완료 | 2026-08-03(2026-08-05 랭크 필터 롤백) | f8c2ad5(task/API-02, PR #9(2026-08-04) main 머지 완료) · 723eb37(fix, 랭크 필터 제거) · `docs/verification/API-02-작업결과.md`, `docs/verification/API-02-rollback-작업결과.md` |
| **API-03** | 조합상세 | GET /catalog/comps/{comp_id} 구현 | 필수 | DATA-02,API-01 | 완료 | 2026-08-03 | 2769e4f(task/API-03, PR #9(2026-08-04) main 머지 완료) · `docs/verification/API-03-작업결과.md` |
| **API-04** | 아이템빌드 | GET /catalog/items/builds 구현 | 필수 | DATA-02,API-01 | 완료 | 2026-08-03 | de02b0d(task/API-04, PR #9(2026-08-04) main 머지 완료) · `docs/verification/API-04-작업결과.md` |
| **API-05** | 증강체정보 | GET /catalog/augments 구현 | 필수 | DATA-07,API-01 | 완료 | 2026-08-04 | 0813e79(feat) · `docs/verification/API-05-작업결과.md` |
| **API-06** | 공통(GNB) | GET /catalog/patches/current 구현 | 필수 | DATA-01,API-01 | 완료 | 2026-08-03 | 3c3a134(task/API-06, PR #9(2026-08-04) main 머지 완료) · `docs/verification/API-06-작업결과.md` |
| **API-07** | 공통 | Rate Limiting 미들웨어 구현 | 필수 | API-01 | 완료 | 2026-08-03 | 25e7935(task/API-07, PR #9(2026-08-04) main 머지 완료) · `docs/verification/API-07-작업결과.md` |
| **API-08** | 챗봇위젯 | session_id 발급/조회 로직 구현 | 필수 | API-01 | 완료 | 2026-08-03 | 4663568(task/API-08, PR #9(2026-08-04) main 머지 완료) · `docs/verification/API-08-작업결과.md` |
| **API-09** | 챗봇위젯 | POST /chat/message 뼈대 구현(SSE) | 필수 | API-08 | 완료 | 2026-08-03 | 37a3803(task/API-09, PR #9(2026-08-04) main 머지 완료) · `docs/verification/API-09-작업결과.md` |
| **API-10** | 챗봇위젯 | GET /chat/session/{id}/history 구현 | 필수 | DATA-03,API-09 | 완료 | 2026-08-03 | b9c95bc(task/API-10, PR #9(2026-08-04) main 머지 완료) · `docs/verification/API-10-작업결과.md` |
| **API-11** | 사후패인분석 | POST /analysis/link 구현 | 필수 | SET-16,API-01 | 대기 | - | - |
| **API-12** | 사후패인분석 | POST /analysis/recent 뼈대 구현 | 필수 | API-11,DATA-03 | 대기 | - | - |
| **API-13** | 분석리포트 | GET /analysis/{match_id}/report 구현 | 필수 | DATA-03,API-12 | 대기 | - | - |
| **API-14** | 공통 | KPI 계측 이벤트 API 구현 | 필수 | DATA-03,API-01 | 완료 | 2026-08-03 | cb96535(task/API-14, PR #9(2026-08-04) main 머지 완료) · `docs/verification/API-14-작업결과.md` |

## B-3. 코딩 > 챗봇 RAG 파이프라인 (CHAT)


| WBS코드 | IA/화면 매칭 | TASK명 | 우선순위 | 선행TASK | 상태 | PM확인 | 커밋/Push |
|---|---|---|---|---|---|---|---|
| **CHAT-01** | 챗봇위젯 | 의도 분류 로직 구현 | 필수 | API-09 | 완료 | 2026-08-03 | b236a82(task/CHAT-01, PR #9(2026-08-04) main 머지 완료) · `docs/verification/CHAT-01-작업결과.md` |
| **CHAT-02** | 챗봇위젯 | 하이브리드 검색 구현 | 필수 | DATA-11,CHAT-01 | 완료 | 2026-08-04 | bda63b2(feat) · `docs/verification/CHAT-02-작업결과.md` |
| **CHAT-03** | 챗봇위젯 | 프롬프트 조립 로직 구현 | 필수 | CHAT-02,API-10 | 완료 | 2026-08-04 | 9556651(feat) · `docs/verification/CHAT-03-작업결과.md` |
| **CHAT-04** | 챗봇위젯 | 입력 전처리 구현 | 필수 | CHAT-01 | 완료 | 2026-08-04 | 848ee1c(task/CHAT-04, PR #9(2026-08-04) main 머지 완료) · `docs/verification/CHAT-04-작업결과.md` |
| **CHAT-05** | 챗봇위젯 | sLLM 답변 생성 및 SSE 스트리밍 연동 | 필수 | CHAT-03,API-09,SET-10 | 완료 | 2026-08-04 | 3f598e4(feat) · `docs/verification/CHAT-05-작업결과.md` |
| **CHAT-06** | 챗봇위젯 | 출력 후처리 구현 | 필수 | CHAT-05 | 완료 | 2026-08-04 | 37af656(feat) · `docs/verification/CHAT-06-작업결과.md` |
| **CHAT-07** | 챗봇위젯 | 웹사이트 링크 자동 삽입 구현 | 필수 | CHAT-06 | 완료 | 2026-08-04 | c6dc992(feat) · `docs/verification/CHAT-07-작업결과.md` |
| **CHAT-08** | 챗봇위젯 | 응답 캐싱 로직 구현 | 필수 | CHAT-05,DATA-15 | 완료 | 2026-08-04 | 8f13b84(feat) · `docs/verification/CHAT-08-작업결과.md` |
| **CHAT-09** | 챗봇위젯 | Q&A 로깅 구현 | 필수 | DATA-03,CHAT-05 | 완료 | 2026-08-04 | e0eb7ad(feat) · `docs/verification/CHAT-09-작업결과.md` |
| **CHAT-10** | 챗봇위젯 | 사후분석 트리거 연동 | 필수 | CHAT-06,API-12 | 대기 | - | - |
| **CHAT-11** | 챗봇위젯 | 후속질문 동적 생성(LLM 기반) 구현 | 권장 | CHAT-05,FE-09 | 대기 | - | - |

## B-4. 코딩 > 사후 패인 분석 (PGA)


| WBS코드 | IA/화면 매칭 | TASK명 | 우선순위 | 선행TASK | 상태 | PM확인 | 커밋/Push |
|---|---|---|---|---|---|---|---|
| **PGA-01** | 사후패인분석 | Riot ID 계정 연동 로직 구현 | 필수 | API-11 | 대기 | - | - |
| **PGA-02** | 사후패인분석 | 최신 매치 조회 로직 구현 | 필수 | PGA-01 | 대기 | - | - |
| **PGA-03** | 사후패인분석·분석리포트 | 분석 결과 캐시 재사용 로직 구현 | 필수 | DATA-03,PGA-02 | 대기 | - | - |
| **PGA-04** | 분석리포트 | 조합 이탈도 계산 로직 구현 | 필수 | PGA-02,DATA-02 | 대기 | - | - |
| **PGA-05** | 분석리포트 | 아이템 집중도 계산 로직 구현 | 필수 | PGA-04 | 대기 | - | - |
| **PGA-06** | 분석리포트 | 증강체 시너지 계산 로직 구현 | 필수 | PGA-04 | 대기 | - | - |
| **PGA-07** | 분석리포트 | 코칭 문장 생성 로직 구현 | 필수 | PGA-04,PGA-05,PGA-06,SET-10 | 대기 | - | - |
| **PGA-08** | 분석리포트 | 다음 게임 제안 근거 조회 로직 구현 | 필수 | PGA-04,DATA-02 | 대기 | - | - |
| **PGA-09** | 분석리포트 | 프라이버시 마스킹 로직 구현 | 필수 | PGA-07 | 대기 | - | - |
| **PGA-10** | 사후패인분석·분석리포트 | 상세 리포트 데이터 조립 로직 구현 | 필수 | PGA-07,PGA-08,PGA-09,API-13 | 대기 | - | - |

## B-5. 코딩 > 프론트엔드 (FE)


| WBS코드 | IA/화면 매칭 | TASK명 | 우선순위 | 선행TASK | 상태 | PM확인 | 커밋/Push |
|---|---|---|---|---|---|---|---|
| **FE-01** | 공통 | Next.js 프로젝트 스캐폴딩 및 Tailwind 설정 | 필수 | SET-07 | 완료 | 2026-08-04 | 25570b9(task/FE-01, PR #10, main 머지 완료) · `docs/verification/FE-01-작업결과.md` |
| **FE-02** | 공통(GNB) | GNB 컴포넌트 구현 | 필수 | FE-01 | 완료 | 2026-08-04 | ad42fca(PR #10, main 머지 완료) · `docs/verification/FE-02-작업결과.md` |
| **FE-03** | 티어리스트 | 티어리스트(홈) 페이지 구현 | 필수 | FE-02,API-02,API-06 | 완료 | 2026-08-04(2026-08-05 랭크 필터 롤백) | d9acb04(feat) · b2e27dd(fix, CORS) · d521f3b(fix, 랭크 필터 UI 제거) · `docs/verification/FE-03-작업결과.md`, `docs/verification/API-02-rollback-작업결과.md` |
| **FE-04** | 조합상세 | 조합 상세 페이지 구현 | 필수 | FE-02,API-03 | 완료 | 2026-08-04 | 18a0115(feat) · 562c478(fix, API-03) · `docs/verification/FE-04-작업결과.md` |
| **FE-05** | 아이템빌드 | 아이템 빌드 페이지 구현 | 필수 | FE-02,API-04 | 완료 | 2026-08-05 | ee2f95b(feat) · 62e1815(fix, API-04) · c146706(fix, batch) · `docs/verification/FE-05-작업결과.md` |
| **FE-06** | 증강체정보 | 증강체 정보 페이지 구현 | 필수 | FE-02,API-05 | 완료 | 2026-08-05 | 8b008ac(feat) · ee67464(fix, API-05) · `docs/verification/FE-06-작업결과.md` |
| **FE-07** | 사후패인분석 | 사후 패인 분석 페이지 구현 | 필수 | FE-02,API-11,API-12 | 대기 | - | - |
| **FE-08** | 분석리포트 | 분석 리포트 상세 페이지 구현 | 필수 | API-13,FE-02 | 대기 | - | - |
| **FE-09** | 챗봇위젯 | 챗봇 위젯 전역 컴포넌트 구현 | 필수 | FE-01,API-09,CHAT-05 | 완료 | 2026-08-06 | 4423c48(feat) · `docs/verification/FE-09-작업결과.md` |
| **FE-10** | 공통 | 이미지 최적화 및 반응형 sizes 적용 | 권장 | FE-03,FE-04,FE-05,FE-06,FE-07,FE-08 | 대기 | - | - |
| **FE-11** | 공통 | 필터 UI 반응형 공용 컴포넌트 구현 | 필수 | FE-01 | 완료 | 2026-08-05 | 0f955ee(feat) · `docs/verification/FE-11-작업결과.md` |
| **FE-12** | KPI 대시보드(비공개) | KPI 대시보드 페이지 구현(비공개) | 필수 | KPI-01 | 대기 | - | - |
| **FE-13** | 티어리스트 | 티어리스트 카드 챔피언 이미지 표시 | 권장 | FE-03 | 완료 | 2026-08-05 | feb8a66(feat, batch) · 1b6ad6b(feat, api) · f18a321(feat, FE-13) · `docs/verification/FE-13-작업결과.md` |
| **FE-14** | 조합상세 | 조합 상세 헥스 배치도 실좌표 반영 | 권장 | FE-04 | 완료 | 2026-08-06 | 9bbae24(feat, api/batch) · 51448ea(feat, comp-detail) · `docs/verification/FE-14-작업결과.md` |

## B-6. 코딩 > KPI/모니터링 (KPI)


| WBS코드 | IA/화면 매칭 | TASK명 | 우선순위 | 선행TASK | 상태 | PM확인 | 커밋/Push |
|---|---|---|---|---|---|---|---|
| **KPI-01** | 공통 | KPI 대시보드 백엔드 API 구현(자체, Metabase 대체) | 필수 | DATA-03 | 완료 | 2026-08-03 | 675db32(task/KPI-01, PR #9(2026-08-04) main 머지 완료) · `docs/verification/KPI-01-작업결과.md` |
| **KPI-02** | 공통 | RAGAS 주간 배치 평가 파이프라인 구현 | 필수 | CHAT-09,SET-08 | 대기 | - | - |
| **KPI-03** | 공통 | 목표 미달 알림 로직 구현 | 권장 | KPI-01 | 보류(이력 저장 테이블·알림 채널 설계 미확정, PM이 추후 재개) | - | - |

## C. 테스트 (TEST)


| WBS코드 | IA/화면 매칭 | TASK명 | 우선순위 | 선행TASK | 상태 | PM확인 | 커밋/Push |
|---|---|---|---|---|---|---|---|
| **TEST-00** | 공통(정책·로직 크리티컬 TASK) | 핵심 정책/로직 테스트 시나리오 사전 정의 | 필수 | SET-01 | 완료 | 2026-08-03 | `docs/test-scenarios.md`, `docs/verification/TEST-00-작업결과.md` |
| **TEST-01** | 공통 | DB 스키마/마이그레이션 검증 | 필수 | DATA-01,DATA-02,DATA-03,DATA-04 | 대기 | - | - |
| **TEST-02** | 티어리스트~증강체정보 | catalog API 단위/통합 테스트 | 필수 | API-02,API-03,API-04,API-05,API-06 | 대기 | - | - |
| **TEST-03** | 챗봇위젯 | chat API 및 RAG 파이프라인 테스트 | 필수 | CHAT-01,CHAT-02,CHAT-03,CHAT-04,CHAT-05,CHAT-06,CHAT-07,CHAT-08,CHAT-09 | 대기 | - | - |
| **TEST-04** | 사후패인분석·분석리포트 | analysis API 및 스코어링 로직 테스트 | 필수 | PGA-01,PGA-02,PGA-03,PGA-04,PGA-05,PGA-06,PGA-07,PGA-08,PGA-09,PGA-10 | 대기 | - | - |
| **TEST-05** | 공통 | 배치 파이프라인 통합 테스트 | 필수 | DATA-05,DATA-08,DATA-09,DATA-10,DATA-11,DATA-12,DATA-13,DATA-14,DATA-15 | 대기 | - | - |
| **TEST-06** | 챗봇위젯 | 챗봇 정책 준수 테스트 | 필수 | CHAT-04,CHAT-06 | 대기 | - | - |
| **TEST-07** | 사후패인분석·분석리포트 | 프라이버시 마스킹 테스트 | 필수 | PGA-09 | 대기 | - | - |
| **TEST-08** | 전체 7화면 | 프론트엔드 E2E 테스트(Playwright) | 필수 | FE-03,FE-04,FE-05,FE-06,FE-07,FE-08,FE-09 | 대기 | - | - |
| **TEST-09** | 전체 7화면 | 반응형 크로스브라우저 테스트 | 필수 | FE-03,FE-04,FE-05,FE-06,FE-07,FE-08,FE-09 | 대기 | - | - |
| **TEST-10** | 공통 | Rate Limiting/보안 테스트 | 권장 | API-07 | 대기 | - | - |

## D. 배포/릴리즈 (REL)


| WBS코드 | IA/화면 매칭 | TASK명 | 우선순위 | 선행TASK | 상태 | PM확인 | 커밋/Push |
|---|---|---|---|---|---|---|---|
| **REL-01** | 전체 | 스테이징 통합 배포 및 QA | 필수 | TEST-01,TEST-02,TEST-03,TEST-04,TEST-05,TEST-06,TEST-07,TEST-08,TEST-09 | 대기 | - | - |
| **REL-02** | 전체 | 프로덕션 배포 | 필수 | REL-01 | 대기 | - | - |
| **REL-03** | 공통 | 배포 후 모니터링 점검 | 필수 | REL-02,KPI-01 | 대기 | - | - |
| **REL-04** | 공통 | 1.0 출시 회고 및 KPI 재보정 계획 수립 | 권장 | REL-03 | 대기 | - | - |
| **REL-05** | 전체 | 포트폴리오 공개 전환 준비 체크리스트 | 권장 | REL-04 | 대기 | - | - |

---


## 스파이크 결과 기록란 (DATA-05, DATA-06, DATA-07)


각 스파이크 완료 시 아래에 결과를 간단히 기록한다(상세 리포트는 `/docs/spike/` 참고).


- **DATA-05 (op.gg MCP 응답 스키마)**: `https://mcp-api.op.gg/mcp`(별도 인증 불필요) 실호출로 6개 도구 전부 확인(2026-08-04). `patch_version` 상당 필드는 도구마다 달라 `tft_list_item_combinations`에만 `version`(예 "17.8") 존재, 나머지 5개는 없음 → 패치 감지는 이 필드 기준으로 Riot 없이 가능. `tft_list_augments`엔 `is_legend_related` 라벨 없음(DATA-07은 수동 목록 필요). `tft_get_play_style`은 PUUID 필수라 배치용이 아님(PM 확인 필요). 상세: `docs/spike/opgg-schema.md`

- **DATA-06 (TFT DDragon 신규 구조)**: 공식 "TFT DDragon" 분리 엔드포인트는 2026-08-04 기준 미확인(추정 도메인 2종 연결 실패, Set 18 런칭 2026-08-12 이후 재확인 필요). 기존 League DDragon `versions.json`은 TFT 패치와 무관한 클라이언트 버전이라 사용 불가(PRD 우려 확인됨). 대안으로 Community Dragon(`raw.communitydragon.org`)이 이미 TFT 전체 데이터 제공 중임을 확인, op.gg와 세트 식별자(TFTSet17) 일치. 상세: `docs/spike/tft-ddragon.md`

- **DATA-07 (is_legend_related 판별 방법)**: op.gg 응답에 라벨 없음 재확인. Community Dragon 전체 세트 검색 결과 "Legend" 계열 증강체는 전부 과거 Set 9 소속이며, Set 17 활성 목록·특성 어디에도 없음 → **Set 17에는 Legends 메커니즘 자체가 없음**. 마스킹 로직 자체는 구현하되(API-05/CHAT-06/FE-06), 목록 관리 인프라는 지금 만들지 않고 `is_legend_related`는 무조건 `false`(PM 결정, YAGNI). 이 정책은 `comps.win_rate`(조합 단위 승률, "S티어 덱"/"리롤 덱" 등 챗봇 질의)와는 무관함을 `policies.md` 1번에 명시 — 상세: `docs/spike/legend-augment.md`


---


## 테스트 시나리오 사전 정의 현황 (TEST-00)


TEST-00 완료 시 아래 TASK들의 테스트 시나리오 문서(`/docs/test-scenarios.md`) 링크·요약을 기록한다. 이 TASK들은 TEST-00에서 정의한 시나리오를 기준으로 단위 테스트를 먼저 작성한 뒤 구현을 진행한다.

PM 승인 완료(2026-08-03) — 상세 결과: `/docs/verification/TEST-00-작업결과.md`


| 대상 TASK | 시나리오 정의 여부 |
|---|---|
| API-05 | 정의됨 |
| CHAT-04 | 정의됨 |
| CHAT-06 | 정의됨 |
| PGA-04 | 정의됨 |
| PGA-05 | 정의됨 |
| PGA-06 | 정의됨 |
| PGA-07 | 정의됨 |
| PGA-09 | 정의됨 |
| DATA-13 | 정의됨 |

---


## 변경 이력


| 일자 | 내용 |
|---|---|
| 2026-07-30 | WBS(90개 TASK) 기준 진행현황.md 최초 작성, 전체 TASK 상태 `대기`로 초기화 |
| 2026-07-30 | WBS에 `테스트 요구사항` 컬럼 추가, SET-14(CI 테스트 게이트)·TEST-00(핵심 시나리오 사전정의) 신규 TASK 반영 — 전체 92개 TASK로 갱신 |
| 2026-07-30 | SET-15(근거 문서 경량 참조본 생성) 신규 반영, `/docs/reference/*.md` 5종 초안 작성 — 전체 93개 TASK로 갱신 |
| 2026-07-30 | REL-05(포트폴리오 공개 전환 준비 체크리스트) 신규 반영(배포릴리즈 그룹 마지막 TASK) — 전체 94개 TASK로 갱신 |
| 2026-07-31 | SET-01 완료(PM 승인) — private 저장소(git@github.com:tigrelli/tft_hideout.git) 생성, main/develop 브랜치 push, .gitignore 구성 |
| 2026-07-31 | SET-02 완료(PM 승인) — Node v20.20.2/pnpm/Python 3.14.4 확인, uv·pre-commit 설치 및 `.pre-commit-config.yaml` 구성 |
| 2026-07-31 | SET-03 완료(PM 승인) — /frontend, /backend, /batch 디렉터리 및 각 README, 루트 README.md 생성 |
| 2026-07-31 | SET-04 완료(PM 승인) — Supabase 프로젝트 생성, pgvector 확장 활성화, Session Pooler 연결문자열로 SELECT 1/vector 확장 스모크 테스트 통과, `.env`/`.env.example` 구성. 자동화 테스트는 이 DB 대신 로컬 Docker(`docker-compose.test.yml`, pgvector/pgvector:pg16)를 쓰기로 결정, `docs/reference/policies.md` 12번 항목 반영 |
| 2026-07-31 | PM 결정 — Redis(Upstash) 제거, 캐싱을 PostgreSQL 테이블(`chat_answer_cache`, `puuid_cache`) 기반으로 전환. 개발설계서 v1.7 반영(구 v1.6은 `/docs/archive/`로 이동). SET-05(Upstash Redis 생성) 취소(코드 결번 유지), SET-09 선행TASK에서 SET-05 제거, DATA-03에 캐시 테이블 2종 추가, DATA-15를 "캐시 정리 배치 구현(Postgres)"로 재정의(선행TASK를 DATA-13,DATA-03으로 변경), CHAT-08·API-11 TASK 설명을 Postgres 기반으로 갱신(WBS.xlsx 동시 반영). 전체 TASK 수 94개 유지(활성 93개, 취소 1개) |
| 2026-07-31 | SET-06 완료(PM 승인) — Render 웹서비스(`tft-hideout-backend`, Singapore) 생성, `render.yaml` Blueprint로 배포 설정. `/health` 엔드포인트만 있는 더미 FastAPI 앱으로 배포 검증(pytest·ruff 통과), `develop`→`main` PR #1 머지 시 자동 재배포 확인, 배포된 Render URL `/health` 200 확인 |
| 2026-07-31 | SET-07 완료(PM 승인) — Cloudflare Workers(정적 assets) `tft-hideout` 생성, Git 연동 자동배포 구성. 최초 `wrangler.toml`을 `pages_build_output_dir`로 설정했으나 프로젝트가 신형 "Workers Builds"로 생성되어 무시됨을 확인, `[assets] directory` 방식으로 수정해 해결. `develop`→`main` PR #2/#3 머지 시 자동 재배포 확인, `workers.dev` 라우트 활성화 후 배포 URL `/` 200 확인 |
| 2026-07-31 | SET-08 완료(PM 승인) — `.github/workflows/manual-smoke.yml` 추가(`workflow_dispatch` + `concurrency` 그룹 패턴). `main`에서 수동 트리거 실행 Success 확인(10초, Node.js 20 지원종료 경고는 무관). 추후 DATA-12·KPI-02는 동일 워크플로우에 `schedule`(cron, UTC 기준) 트리거를 함께 추가하는 패턴을 따르기로 함(스케줄 트리거는 `main` 워크플로우 파일만 인식, 60일 미활동 시 자동 비활성화 유의) |
| 2026-07-31 | SET-09 완료(PM 승인) — `DATABASE_URL`을 GitHub Actions 시크릿·Render 환경변수로 등록, 값 노출 없이 참조 확인(`/env-check`, 워크플로우 로그). Cloudflare는 정적 assets 전용 Worker라 환경변수 미지원 확인 → FE-01로 이관. SET-* 인프라 검증 상세 기록을 `/docs/verification/smoke-tests.md`로 분리(CLAUDE.md v1.6 반영) |
| 2026-08-03 | SET-10 보류 처리 — Riot API 키 발급 진행 중이라 발급 완료 시까지 홀딩, 상태를 `진행중(Riot API 키 발급 대기)`로 표시하고 WBS 순서상 다음 착수 가능 TASK인 SET-11로 이동 |
| 2026-08-03 | SET-11 시도 실패 — Metabase를 Render 무료 플랜(512MB)에 배포 시도했으나 OOM으로 실패(`JAVA_OPTS=-Xmx400m` 적용해도 실패). 상세는 `/docs/verification/smoke-tests.md` 참고 |
| 2026-08-03 | PM 결정 — Metabase를 제외하고 KPI 대시보드를 자체 구현 페이지(`/kpi`, 비밀번호 게이트, GNB 미노출)로 전환. SET-11(Metabase 배포) 취소, KPI-01을 "KPI 대시보드 백엔드 API 구현(자체)"으로 재정의(선행TASK에서 SET-11 제거), 신규 FE-12(KPI 대시보드 페이지 구현) 추가 — WBS.xlsx 동시 반영, 전체 95개 TASK로 갱신(취소 2개: SET-05, SET-11). IA를 v1.1→v1.2로 갱신(3.1 페이지 인벤토리·4.7 신규 절·6.1 GNB·7장 URL 구조·8장 화면-데이터 매핑에 `/kpi` 반영, 구버전은 `/docs/archive/`로 이동). CLAUDE.md 3장 기술스택 표도 Metabase 항목을 자체 구현으로 갱신 예정 |
| 2026-08-03 | SET-12 보류 처리 — Figma "TFT_Hideout" 와이어프레임 파일이 아직 없어 Figma MCP 연동 스모크 테스트(get_design_context 호출) 자체가 불가능함을 확인. PM이 Figma에 와이어프레임을 작성한 뒤 재개하기로 함(설정 파일(.mcp.json) 준비도 보류). WBS 순서상 다음 착수 가능 TASK인 SET-13으로 이동 |
| 2026-08-03 | SET-13 완료(PM 승인) — CLAUDE.md 4장에 PM 승인 후 커밋/push 규칙이 이미 명문화되어 있음을 확인(추가 변경 불필요), `.github/PULL_REQUEST_TEMPLATE.md` 신규 작성(WBS 코드·테스트 체크리스트·PM 확인 체크리스트) |
| 2026-08-03 | SET-14 완료(PM 승인) — `.github/workflows/ci.yml` 신설(backend pytest+ruff, frontend Vitest는 FE-01 전까지 조건부 스킵). 더미 실패 테스트로 PR 체크 실패(빨간 X)까지는 확인했으나, GitHub 무료 플랜(Private 저장소)은 브랜치 보호 규칙이 "설정은 되나 강제되지 않음"을 확인(smoke-tests.md 기록) — PM 결정으로 유료 전환 없이 소프트 게이트(CI 표시 + 기존 PM 승인 절차)로 운영하기로 하고 WBS DoD 문구 갱신. 더미 테스트 제거 후 PR #8 전체 체크 통과 확인 및 main 머지 완료 |
| 2026-08-03 | SET-15 소급 완료 처리(PM 승인) — `/docs/reference/*.md` 5개 파일이 이미 2026-07-30 최초 커밋(686b279)에 포함되어 실제로 사용 중이었으나 진행현황.md 갱신이 누락되어 있던 것을 확인, 대조 확인된 것으로 보고 완료 처리 |
| 2026-08-03 | PM 결정 — DATA-*(B-1 데이터 파이프라인) 계열 TASK는 로컬 pytest로 자체 검증되고 main에 실제로 반영된 상태를 봐야 확인되는 DoD가 아니므로, 매 TASK마다 즉시 main 머지하지 않고 여러 TASK를 모아 배치로 머지하기로 함(SET-06~08처럼 배포 확인이 필요한 TASK는 기존대로 즉시 머지) |
| 2026-08-03 | DATA-01 완료(PM 승인) — SQLAlchemy 모델(db/models.py) + Alembic 마이그레이션으로 patches/champions/traits/champion_traits/items/augments 6개 정적 테이블 생성, pytest 8종(테이블·컬럼·JSONB 타입·FK/UNIQUE 제약)으로 검증. CI(backend-tests)에 Postgres 서비스 컨테이너 추가 |
| 2026-08-03 | DATA-02 완료(PM 승인) — comps/comp_champions/comp_augments/champion_item_builds 4개 메타조합 테이블 마이그레이션(DATA-01 위에 체이닝), comp_champions/comp_augments FK 위반 시 IntegrityError 발생 확인하는 pytest 포함 |
| 2026-08-03 | DATA-03 완료(PM 승인) — match_analyses/chat_logs/link_click_events/account_link_events/patch_detection_runs/ragas_eval_results/chat_answer_cache/puuid_cache 8개 테이블 마이그레이션. chat_answer_cache/puuid_cache는 개발설계서 v1.7 4.6절에 컬럼 단위 명세가 없어 캐시 전략(키·TTL) 설명 기반으로 이번에 직접 확정하고 schema.md에 반영 |
| 2026-08-03 | DATA-04 완료(PM 승인) — meta_document_embeddings 테이블(embedding vector(1024)) + HNSW(vector_cosine_ops, m=16/ef_construction=64) 인덱스 + (patch_version,doc_type) btree 복합 인덱스 마이그레이션. 더미 벡터 코사인 검색 순서 확인 + enable_seqscan=off 상태 EXPLAIN으로 HNSW 인덱스 사용 확인 |
| 2026-08-03 | PM 결정 — SET-10(Riot API 키) 발급 대기 중이라, 이에 의존하는 DATA-05~09/11~13·API-05/11~13은 건너뛰고 Riot 키와 무관한 API-*(API-01/02/03/04/06/07/08/09/10/14)부터 진행하기로 함 |
| 2026-08-03 | API-01 완료(PM 승인) — catalog/chat/analysis/kpi 4개 APIRouter 스캐폴딩(kpi는 SET-11 취소로 자체 KPI 대시보드 백엔드용 추가, WBS 원문 3개→4개로 갱신). pytest로 4개 라우터 마운트 확인 |
| 2026-08-03 | API-02 완료(PM 승인) — GET /api/v1/catalog/tierlist 구현(patch/rank 필터, patch 미지정 시 현재 패치 기본값). API-01의 라우터 prefix가 api-spec.md 기준(/api/v1/...)과 달라 이번에 수정. comps에 rank_tier 컬럼 추가(기본값 "all", 실제 랭크 구간 값은 DATA-05 스파이크 후 확정 필요). 공통 에러 응답 포맷 핸들러({"error":{"code","message"}}) main.py에 추가 — 이후 API-* TASK 공용 |
| 2026-08-03 | API-03 완료(PM 승인) — GET /api/v1/catalog/comps/{comp_id} 구현(챔피언 구성 캐리 우선 정렬 + 추천 증강체 priority 순 정렬), 존재하지 않는 comp_id는 404 |
| 2026-08-03 | API-04 완료(PM 승인) — GET /api/v1/catalog/items/builds 구현(champion_id 필터, play_rate 내림차순 정렬). patch 기본값 해석 로직을 _resolve_patch()로 공용화 |
| 2026-08-03 | API-06 완료(PM 승인) — GET /api/v1/catalog/patches/current 구현(is_current=true 1건 조회, 미설정 시 404). catalog 그룹(API-02~04,06) Riot 키 무관 TASK 완료 |
| 2026-08-03 | API-07 완료(PM 승인) — Rate Limiting 미들웨어 구현(IP 기준 catalog 분당60/chat 분당10, 프로세스 메모리 고정윈도, 별도 인프라 없음). 테스트 간 상태 격리를 위해 conftest.py에 autouse 리셋 픽스처 추가 |
| 2026-08-03 | API-08 완료(PM 승인) — session_id UUID 검증 + session별 chat_logs 조회 로직(services/chat_session.py), API-09/10이 재사용할 기반 |
| 2026-08-03 | PM이 .env에 RIOT_API_KEY 등록 → Groq/HF 키와 함께 실호출 테스트 결과 Groq·HF는 200 정상, Riot는 401 "Unknown apikey"(Development API Key 24시간 만료 추정) — PM이 키 재발급 확인 예정, SET-10은 계속 보류 |
| 2026-08-03 | API-14 완료(PM 승인) — KPI 계측 이벤트 API 구현. link_click_events는 POST /api/v1/chat/events/link-click 공개 엔드포인트로, account_link_events는 훅 함수만 구현(API-11/12가 추후 호출) |
| 2026-08-03 | API-09 완료(PM 승인) — POST /api/v1/chat/message SSE 스트리밍 뼈대 구현(services/chat_stream.py). RAG 로직(의도분류·검색·프롬프트조립) 없이 배관만 우선 구축, mock_llm_stream은 CHAT-05가 Groq 실연동으로 교체할 자리표시자. session_id 검증은 API-08 로직 재사용 |
| 2026-08-03 | API-10 완료(PM 승인) — GET /api/v1/chat/session/{id}/history 구현(드릴다운용 최근 3턴만 반환). get_session_history에 limit 파라미터 추가(RECENT_TURNS_LIMIT=3), 기존 API-08 전체조회 동작은 하위호환 유지 |
| 2026-08-03 | CHAT-01 완료(PM 승인) — 의도 4분류 로직 구현(1차 키워드/정규식, 애매할 때만 2차 Groq LLM). services/groq_client.py는 프로젝트 최초의 실제 Groq API 연동 코드(requirements.txt에 groq SDK 추가), LLM 실패/무효응답은 general_strategy로 폴백. classify_intent_for_query는 아직 미배선 — 엔드포인트 연동은 CHAT-03~05에서 이어짐 |
| 2026-08-03 | KPI-01 완료(PM 승인) — GET /api/v1/kpi/summary(5개 지표: 데이터최신성/근거율/전환율/이용률/응답지연) + POST /api/v1/kpi/auth(비밀번호→서명토큰, TTL 12시간) 구현. PM 결정: 전환율 모수는 CHAT-07 미구현 상태라 answer 텍스트 내부경로 패턴 탐지로 임시 산정(CHAT-07 구현 후 재검토 필요). KPI_DASHBOARD_PASSWORD .env 등록 완료(PM) |
| 2026-08-03 | PM 결정 — KPI-03(목표 미달 알림 로직, 권장)은 이력 저장 테이블·알림 채널 설계가 미확정이라 보류, 추후 PM이 설계 확정 후 재개 |
| 2026-08-03 | TEST-00 완료(PM 승인) — `/docs/test-scenarios.md`에 API-05/CHAT-04/CHAT-06/PGA-04~07·09/DATA-13 9개 TASK 입력값·기대출력·경계값 표 작성. 상세 결과·설계 중 확정한 경계조건 5건은 `/docs/verification/TEST-00-작업결과.md` 참고 |
| 2026-08-03 | PM 요청 반영 — 완료 TASK 결과를 `docs/verification/{코드}-작업결과.md`로 기록하는 컨벤션 도입(CLAUDE.md v1.8). 기존 완료 TASK 27건(SET-01~04·06~09·13~15, DATA-01~04, API-01~04·06~10·14, CHAT-01, KPI-01)에 소급 적용해 결과 파일 생성 및 진행현황.md 커밋/Push 컬럼에 링크 추가 |
| 2026-08-04 | CHAT-04 완료(PM 승인) — 입력 전처리 구현(정규화·은어치환·인젝션 구조적 분리·길이제한·범위밖질문/명확화 플래그·대화이력 3턴 제한). TEST-00 시나리오 7개 케이스 그대로 pytest 13건으로 옮김. `is_off_topic`/`needs_clarification` 플래그는 값 계산까지만 담당하며 실제 응답 분기 배선은 CHAT-05 이후로 이어짐. PM 결정: main 머지는 B-5(프론트엔드 착수) 직전에 그동안의 task 브랜치들과 함께 일괄 진행 |
| 2026-08-04 | PM 결정 — SET-12(Figma 연동) 취소. 이 세션(Claude Code) 환경에 Figma MCP 커넥터가 연결되어 있지 않아(`get_design_context` 등 도구 자체가 없음) DoD(Figma MCP 호출 테스트 성공)를 검증할 수 없는 상태였고, PM이 Figma MCP를 사용하지 않기로 결정. FE-01 선행TASK에서 SET-12 제거(SET-07만 남음). 이후 FE-* 작업은 CLAUDE.md 6장 순서대로 `/docs/reference/design-tokens.md`와 화면설계서(`/docs/source/TFT_sLLM_화면설계서_v1.2.docx`)만을 디자인 참조 소스로 삼는다(Figma는 참조하지 않음). WBS.xlsx SET-12 행도 취소 사유로 갱신 |
| 2026-08-04 | PM 요청 반영 — CLAUDE.md v1.10: TASK별 브랜치 생성 방식 폐기, 다음 배치 머지 시점까지 작업 브랜치 1개를 이어가는 방식으로 전환(4장 브랜치 규칙 갱신, `develop` 브랜치 미사용 명시). main 직접 push는 문서 전용/PM 승인 완료 커밋 예외로 한정 |
| 2026-08-04 | FE-01 완료(PM 승인) — Next.js 16(App Router)+TypeScript strict+Tailwind v4 스캐폴딩. Cloudflare가 정적 assets 전용이라 `output: "export"`로 빌드, `wrangler.toml` 배포 대상을 `frontend/out`으로 변경(PM이 Cloudflare 대시보드 Build command·NODE_VERSION 설정 완료). `design-tokens.md` 컬러/타이포/반경을 Tailwind `@theme`로 이식, 브레이크포인트는 Tailwind 기본값 그대로. 임시 헤더 셸(`header-shell.tsx`, 실제 GNB는 FE-02) + Vitest/RTL 14개 테스트(브레이크포인트 클래스, 토큰 색상값 매칭). CI(SET-14) frontend job을 pnpm→npm 전환하고 lint/format/test/build로 보강(그동안 스킵되던 게 활성화됨). PM이 로컬 `npm run dev`로 직접 화면 확인 후 승인 |
| 2026-08-04 | FE-02 완료(PM 승인) — GNB 컴포넌트 구현(4개 진입점: 티어리스트/아이템 빌드/증강체 정보/전적 분석). 데스크톱·태블릿은 가로 메뉴(현재 페이지 Bold 강조), 모바일은 햄버거→전체화면 드로어. GNB 로고 텍스트는 디자인가이드의 "TFT Meta" 대신 프로젝트명 "TFT Hideout" 유지로 확정(`design-tokens.md`에 기록). 우측 "현재 패치 배지"(디자인가이드 6.1)는 백엔드 연동이 필요해 이번 TASK 범위에서 제외 — 추가 시점 PM 확인 필요. Vitest+RTL 19개 + Playwright e2e 스모크 1개(신규 `frontend/e2e/`, `playwright.config.ts`) 통과. FE-03 착수 전 정리 필요 사항 발견: FE-01의 정적 export(`output: "export"`) 결정과 WBS FE-03의 "ISR 렌더링" 설명이 서로 모순 — PM 확인 필요 |
| 2026-08-04 | PM 결정 — 사후 패인 분석(PGA)만 Riot API 키가 필요하고 티어리스트/아이템빌드/증강체정보 등 카탈로그 데이터는 op.gg MCP만으로 충분하다는 판단에 따라 SET-10을 분리: SET-10은 Groq·HF 키 검증만으로 재정의(2026-08-03에 이미 200 확인됨, PM확인대기로 표시), Riot Personal Key는 신규 SET-16으로 분리(진행중, 기존 Development Key 401 만료로 재발급 대기). DATA-05(op.gg 스키마 확인)·DATA-06(TFT DDragon 확인)은 op.gg/DDragon 모두 키 불요로 선행TASK 제거해 즉시 착수 가능 상태로 전환, API-11(Riot ID→PUUID)만 SET-16 의존으로 변경. DATA-11·CHAT-05·PGA-07은 Groq/HF가 필요해 SET-10 의존 유지. WBS.xlsx 동시 반영(SET-16 신규, 전체 96개 TASK) |
| 2026-08-04 | SET-10 완료(PM 승인) — Groq·HF 키 검증(2026-08-03 200 확인 결과를 근거로 분리 후 확정 승인) |
| 2026-08-04 | PR #10(task/FE-01 → main) 머지 완료 — FE-01·FE-02·SET-10(분리)·DATA-05~13 총 15개 TASK 일괄 반영. 머지 전 CI에서 `batch-tests`가 두 차례 실패해 원인 조사·수정: (1) batch가 실제 DB(migrated_engine)를 쓰게 된 DATA-10부터 CI의 `batch-tests` job에 postgres 서비스 컨테이너가 빠져있던 걸 발견해 추가, (2) `.pre-commit-config.yaml`은 ruff v0.14.0 고정인데 backend/batch `requirements-dev.txt`는 버전 미고정이라 pip install 시 최신(0.16.1)이 깔려 E402/PLR0402 판정이 달라지는 걸 발견해 양쪽 다 `ruff==0.14.0`으로 고정. PM이 `gh` CLI용 Personal Access Token 발급 → 직접 Actions 로그 조회·재실행까지 확인해 CI 전체 통과 후 머지. 진행현황.md 정리 중 FE-02가 승인까지 됐는데 상태 표만 "대기"로 안 바뀌어 있던 걸 발견해 함께 수정(작업결과 문서·이력 로그엔 이미 완료로 기록돼 있었음 — 표만 누락) |
| 2026-08-04 | DATA-14 자체검증 완료(PM확인대기) — 착수 전 PM 확인: 실제 운영 DB에 매시간 쓰는 첫 자동화라 워크플로우는 만들되 `schedule`은 아직 켜지 않고 `workflow_dispatch`만 활성화하기로 결정. `batch/run_patch_batch.py`로 DATA-12~13을 실제로 배선(DATA-08/09 수집→DATA-10 정규화→DATA-11 임베딩→DATA-13 원자적 전환), 로컬 테스트 DB로 실제 실행 성공 확인. `.github/workflows/patch-detection.yml` 신규(`concurrency: cancel-in-progress: false`로 취소 대신 대기열 방식). concurrency 자체는 GitHub 서버 기능이라 로컬 검증 불가 — main 머지 후 PM이 workflow_dispatch 2연속 트리거로 직접 확인 필요. HUGGINGFACE_API_KEY GitHub 시크릿 등록 여부도 미확인(PM 확인 필요) |
| 2026-08-04 | DATA-13 완료(PM 승인) — TEST-00 DATA-13 사전 시나리오 5개를 그대로 pytest로 옮김. `batch/patch_transition.py`의 `promote_patch_to_current()`(단일 트랜잭션 승격) + `run_batch_with_atomic_promotion()`(단계 실패 시 승격 자체를 건너뛰어 이전 패치 유지, 예외는 삼키고 `patch_detection_runs`에 success/failed 기록). pytest 66/66(트랜잭션 격리 수준까지 검증: 커밋 전/후 다른 커넥션이 보는 값 차이 확인). 추가로 DATA-08~13을 실제로 전부 연결해 op.gg+Community Dragon 실호출→정규화→임베딩→원자적 승격까지 9.9초 만에 성공 확인 — 전체 배치 파이프라인이 처음으로 하나로 이어져 동작함이 증명됨 |
| 2026-08-04 | DATA-12 완료(PM 승인) — `batch/patch_detection.py`로 `tft_list_item_combinations().version`(DATA-05 결정) 기준 패치 감지 구현. `run_patch_detection()`이 DB `is_current` 버전과 op.gg 최신 버전을 비교해 다르면 주입받은 `on_trigger` 콜백 호출, `patch_detection_runs`에 실행 로그 기록. 실제 전체 배치 재수집 오케스트레이션 연결은 DATA-13 이후로 미룸(콜백 자리만 마련). pytest 60/60 + 실제 op.gg로 가짜 옛날 버전→트리거 발동, 최신 버전 승격 후 재검사→트리거 미발동 2단계 시뮬레이션 확인 |
| 2026-08-04 | DATA-11 완료(PM 승인) — 착수 전 발견 2가지: (1) HF Inference API 레거시 엔드포인트(`api-inference.huggingface.co`)가 DNS부터 안 떠서 신규 라우터 엔드포인트(`router.huggingface.co/hf-inference/models/BAAI/bge-m3/pipeline/feature-extraction`)로 실호출 확인, (2) `meta_document_embeddings`도 DATA-10과 동일하게 upsert용 UNIQUE 제약이 없어 마이그레이션 추가(기존 DATA-04 테스트 fixture 충돌 수정). `batch/embeddings.py`로 comp/playstyle/augment/item_build 4종 chunk 생성 + BGE-M3 임베딩 + upsert 구현. pytest 51/51 + backend 회귀 100/100 + 실제 HF API로 30개 chunk 임베딩(1024차원)·upsert·재수집 중복없음 확인 + 실제 pgvector 코사인 유사도 검색으로 자기 자신이 1위로 나오는 것까지 확인 |
| 2026-08-04 | DATA-10 완료(PM 승인) — 착수 전 스키마 문제 발견: upsert에 필요한 (patch_version, 원본ID) UNIQUE 제약이 champions/items/augments엔 없고 traits/comps는 원본ID 컬럼 자체가 없어 마이그레이션 추가(PM 승인). comps 정규화 3가지 결정(PM 승인): tier_rank는 op.gg 값 그대로, playstyle_text는 badge+캐리챔피언으로 자동생성(LLM 미사용), comp_augments는 데이터 소스 없어 이번엔 제외. `batch/normalize.py`로 champions/traits/items/augments/comps/comp_champions/champion_item_builds upsert 구현. pytest 38/38(순수 변환 + 실제 마이그레이션 DB로 upsert 검증) + backend 회귀 100/100 + 실제 op.gg·Community Dragon 데이터로 2회 연속 적재해 재수집 시 행 개수 동일함(중복 없음) 확인 |
| 2026-08-04 | DATA-09 완료(PM 승인) — `batch/id_name_mapping.py` 신규(Community Dragon `raw.communitydragon.org` 기준, DATA-06 결론). `CommunityDragonClient.fetch_tft_data()` + `build_name_maps()`로 세트별 챔피언·특성 apiName→이름 매핑, 매핑 누락 시 세트 접두어 제거 폴백(예외 없음). pytest 20/20(기존 10+신규 10) + 실제 Community Dragon 1회 접속해 Set 17 챔피언 83개·특성 44개 매핑 확인(ko/en 둘 다) — pytest 스위트엔 미포함 |
| 2026-08-04 | DATA-08 완료(PM 승인) — `batch/opgg_client.py` 신규(`OpggMcpClient`, 5개 TFT 도구 순차 호출, MCP 세션 핸드셰이크·재시도·에러 처리). pytest 10/10(DATA-05 스파이크 기반 합성 fixture, 실 API 미호출) + 완성된 클라이언트로 실제 op.gg MCP 1회 실호출해 5개 도구 전부 성공 확인(WBS DoD 별도 검증, pytest 스위트엔 미포함). batch 첫 코드 TASK라 CI에 `batch-tests` job 신규 추가 |
| 2026-08-04 | DATA-07 완료(PM 승인) — op.gg 라벨 없음 재확인 + Community Dragon 전체 세트 검색 결과 "Legend" 계열 증강체 5개 전부 과거 Set 9 소속이고 Set 17 활성 목록·특성엔 없음을 확인 → Set 17에 Legends 메커니즘 자체가 없다고 판단. PM 피드백으로 결론 단순화: 목록 관리 인프라는 지금 만들지 않고 `is_legend_related`는 무조건 `false`(YAGNI), 마스킹 로직 자체는 구현. Riot 정책의 실제 금지 대상(Legend 자체/전용증강 승률)과 `comps.win_rate`(조합 단위, "S티어 덱" 등 챗봇 질의)는 무관함을 `policies.md` 1번에 명시 — 조합 승률은 기존 의도 4분류(조합 추천)로 이미 커버되며 별도 정책 불필요. 상세: `docs/spike/legend-augment.md` |
| 2026-08-04 | DATA-06 완료(PM 승인) — Community Dragon 기준으로 DATA-09 설계 진행 확정, Set 18 런칭(2026-08-12) 이후 공식 TFT DDragon 등장 여부는 그때 재확인하기로 함(DATA-01~15 표 하단에 메모 남김) — 공식 "TFT DDragon" 분리 엔드포인트는 추정 도메인 2종 모두 연결 실패로 아직 미확인(Set 18 런칭 2026-08-12 이후 재확인 필요). 기존 League DDragon `versions.json`은 TFT 패치와 무관한 클라이언트 버전(예 "16.15.1")이라 사용 불가 — PRD 우려 그대로 확인됨(단, DATA-05에서 이미 op.gg 기준으로 패치 감지를 확정해 실질 영향 없음). 대안으로 Community Dragon(`raw.communitydragon.org`)이 TFT 전체 데이터를 이미 제공 중임을 확인, `setData[].mutator`("TFTSet17")가 op.gg `teamCode` 접미사와 일치 + op.gg 응답 자체가 `type: "cdragon-item"`이라 같은 소스 사용을 시사 → DATA-09 ID-이름 매핑에 Community Dragon 사용 제안. 상세: `docs/spike/tft-ddragon.md` |
| 2026-08-04 | DATA-05 완료(PM 승인) — `https://mcp-api.op.gg/mcp`로 실접속(별도 인증 불필요), 6개 TFT 도구 전부 실호출해 응답 스키마 확인. PM 결정 3가지: (1) 패치 감지(DATA-12)는 `tft_list_item_combinations.version` 필드 기준으로 확정(Riot 키 없이 가능, 나중에 리더보드 샘플링을 보조 신호로 추가 가능), (2) `is_legend_related` 라벨은 op.gg에 없음 확정 → DATA-07은 수동 목록 방식(용도: Riot TFT 정책상 Legend 계열 증강체 승률 표시 금지를 지키기 위한 마스킹 대상 플래그), (3) `tft_get_play_style`(PUUID 필수, 개인 데이터 도구)은 DATA-08에서 제외하고 PGA-07로 이동 — WBS.xlsx DATA-08("5개 도구")·PGA-07 TASK 설명 갱신, `glossary.md`·`api-spec.md`의 "개인 전적 조회 기능 없음" 문구도 수정. 상세: `docs/spike/opgg-schema.md` |
| 2026-08-04 | PR #10 머지 후 GitHub Actions 배치 첫 실제 실행에서 프로덕션 버그 2건 발견해 main에 직접 push로 긴급 수정(WBS 코드 없음, 이전 세션에서 누락됐던 기록 소급 반영): **f5f9b49** `DATABASE_URL` 드라이버 미지정(`postgresql://`) 시 psycopg2로 잘못 해석돼 `ModuleNotFoundError` 발생 → `backend/db/session.py`·`batch/db_session.py`에 `postgresql+psycopg://` 자동 보정 추가(pytest 검증 포함). **ed3f1e3** 운영 Supabase DB에 `relation "patches" does not exist` 발견(SET-06 이후 스키마가 한 번도 생성된 적 없었음) → `render.yaml` buildCommand에 `alembic upgrade head` 추가 시도 |
| 2026-08-04 | DATA-14 트리거 검증 실시(PM 요청) — (1) `HUGGINGFACE_API_KEY` GitHub Secrets 등록 재확인 완료. (2) `workflow_dispatch` 2연속 트리거로 동시 실행 방지(대기열) 정상 동작 확인(`cancel-in-progress: false`대로 두 번째가 취소 없이 첫 번째 뒤에 순차 실행). (3) 그 과정에서 위 `ed3f1e3` 수정이 실제로는 배포에 반영되지 않았음을 발견 — 원인은 Render 서비스 Root Directory(`backend`) 밖의 `render.yaml` 변경이 auto-deploy 트리거 대상이 아니었던 것 + Blueprint 서비스는 `render.yaml` 변경이 있어도 Build Command가 자동 동기화 안 되는 것 2가지 중첩. PM이 Render 대시보드에서 Build Command를 직접 `pip install -r requirements.txt && alembic upgrade head`로 수정해 재배포 트리거. 이어서 **`alembic/env.py`가 `f5f9b49`의 드라이버 보정을 안 타서** 또 `ModuleNotFoundError: psycopg2`로 빌드 실패하는 것 추가 발견 → `get_database_url()` 재사용하도록 수정(커밋 227178c, pytest 104/104 통과) 후 main push. 재배포 후 `GET /api/v1/catalog/patches/current` 500→404(스키마 생성 확인)로 바뀌었고, 이어서 `workflow_dispatch` 재트리거한 배치가 **처음으로 전체 성공**(패치 17.8 승격), 같은 엔드포인트가 200으로 실데이터 응답까지 확인 — 운영 Supabase DB에 스키마·데이터가 이번에 처음 실제 생성됨. 상세: `docs/verification/DATA-14-작업결과.md`, `docs/verification/SET-06-작업결과.md`(Render Blueprint 주의사항 추가) |
| 2026-08-04 | DATA-14 완료(PM 승인) — `schedule` 트리거 활성화(매일 03:00 KST = `cron: "0 18 * * *"`). PRD 9-1 원안(매시간)이 아니라 1일 1회로 결정: TFT 패치는 월 1~2회뿐이라 매시간 감지는 GitHub Actions 무료 분(2,000분/월, CI와 공유)의 약 36%(720분/월)를 소모하는 반면 1일 1회는 약 1.5%(30분/월)만 소모, 개인/지인 10명 규모 MVP에서 최대 1시간 지연의 이점이 예산 절감보다 크지 않다고 판단. 패치 발생 후 최대 24시간 지연은 `workflow_dispatch` 수동 트리거로 즉시 만회 가능(이미 동작 확인됨) |
| 2026-08-04 | Riot API 키(SET-16) 제외하고 진행 가능한 백엔드 TASK 재검토 — API-05·CHAT-02~09(일부)·DATA-15·KPI-02가 아직 대기 상태로 남아있었음을 확인(2026-08-03 "Riot 키 무관 API부터" 결정 당시엔 DATA-07 미완료로 API-05가 막혀 있었으나 08-04에 DATA-07이 끝나면서 지금은 막힘 없음). WBS 순서(그룹→하위영역→번호)상 데이터파이프라인 그룹의 DATA-15가 백엔드API 그룹보다 먼저이므로 DATA-15부터 재개하기로 함 |
| 2026-08-04 | DATA-15 완료(PM 승인) — `batch/cache_cleanup.py`로 `delete_stale_chat_answer_cache()` 구현(chat_answer_cache에서 현재 patch_version과 다른 행 DELETE). `puuid_cache`는 자체 `expires_at` TTL이라 WBS 완료기준·`db/models.py` 독스트링 모두 이 TASK 범위에서 제외함을 확인하고 그대로 반영. `run_patch_batch.py`의 `on_trigger()`에서 배치 승격이 **성공했을 때만** 정리를 실행하도록 배선(실패 시 이전 유효 버전 캐시가 잘못 삭제되지 않게). pytest 4건 신규(batch 74/74 통과, WBS 테스트요구사항 "배치 완료 이벤트 발생 시 삭제 확인" 그대로 옮김) |
| 2026-08-04 | API-05 완료(PM 승인) — 착수 전 발견: TEST-00 시나리오가 전제한 "증강체 win_rate"가 실제로는 없었음(`augments` 테이블에 컬럼 자체가 없었고, op.gg `tft_list_augments`도 win_rate를 안 줌(DATA-05 스파이크), Riot 공식 API도 확인해보니 매치 원본 데이터만 제공하고 집계 통계는 없어 대체 불가). PM 승인(DATA-07과 동일 패턴): `augments.win_rate`(nullable) 컬럼만 추가하고 배치는 채우지 않음(항상 NULL), 마스킹 로직은 TEST-00대로 구현. `GET /api/v1/catalog/augments?patch=&tier=` 구현, `is_legend_related=true`면 DB 값과 무관하게 응답 `win_rate`를 무조건 `null`로 강제. pytest 7건 신규(TEST-00 시나리오 4건 포함), backend 111/111·batch 74/74 통과 |
| 2026-08-04 | CHAT-02 완료(PM 승인) — `services/hybrid_search.py`로 glossary.md 챗봇 의도 4종(조합/아이템/증강체 추천, 일반 전략)별 `doc_type` SQL 필터 + pgvector `cosine_distance()` top-k 정렬 구현. `services/embedding_client.py` 신규(실시간 질문 임베딩용 HF 클라이언트) — Render backend는 `rootDir: backend`라 `batch/embeddings.py`를 임포트할 수 없어 필요한 부분만 복제. CHAT-01 의도 분류 결과를 입력으로 받는 구조만 마련, 엔드포인트 배선은 CHAT-03/05에서 진행(API-09와 동일한 "뼈대 우선" 패턴). pytest 10건 신규(backend 121/121 통과), 외부 HF API 미호출(mock) |
| 2026-08-04 | CHAT-03 완료(PM 승인) — 착수 전 설계서 4.4.1 원본(.docx) 직접 대조(경량 참조본엔 이 절 요약 없음): 시스템 프롬프트 7규칙·의도별 추가지시 표·few-shot 권장사항 확인해 그대로 반영. `services/prompt_assembly.py`로 시스템프롬프트(+의도별 지시) → few-shot(합성 예시) → `[검색된 문서]`(기준 패치 명시) → `[이전 대화]`(있을 때만) → CHAT-04 `wrap_user_message()` 결과 순서로 조립. WBS 요구사항인 전체 조합 스냅샷 테스트 포함 pytest 7건 신규(backend 128/128 통과) |
| 2026-08-04 | CHAT-05 완료(PM 승인) — `services/groq_client.py`에 `stream_groq_chat()` 추가(Groq `stream=True`, 델타 토큰 순서대로 yield). `services/prompt_assembly.py`의 `assemble_prompt()`를 `assemble_system_turn()`/`assemble_user_turn()`으로 분해(Groq system/user 역할 분리 호출 지원, 기존 스냅샷 테스트는 변경 없이 통과 확인). `services/chat_stream.py` 전면 재작성: `stream_llm_answer()`가 토큰 없이 실패 시 1회 재시도, 그래도/도중 실패 시 예외 전파 없이 고정 폴백 메시지로 대체. `generate_answer_stream()`이 CHAT-04가 미뤄뒀던 `is_off_topic`/`needs_clarification` 분기를 실제 연결하고, 정상 질문은 현재패치조회(신규 `current_patch.py`)→CHAT-01 의도분류→CHAT-02 임베딩+검색→CHAT-03 프롬프트조립→스트리밍 순으로 배선(외부 API 호출부는 명시적 인자 주입). `routers/chat.py`가 API-09 mock 배관을 실제 파이프라인으로 완전 대체, API-09 테스트에서 mock 특정 동작 의존 테스트 4건 정리. pytest 10건 신규(backend 135/135 통과), 외부 API 미호출(fake 주입) |
| 2026-08-04 | CHAT-06 완료(PM 승인) — 착수 전 PM과 근거검증 메커니즘 논의: 자유문장 NER은 정규식만으로 불가능하다는 점 확인 후, 시스템 프롬프트에 8번 규칙 신설("조합/챔피언/아이템/증강체는 작은따옴표로 인용")로 해결 — LLM 자연어 이해는 여전히 프롬프트가 전담, 후처리는 결정론적 안전망(설계서 4.4.3 "문자열 매칭" 원문 그대로, LLM 재검증 호출 없음). `services/chat_postprocessing.py` 신규: `verify_grounding()`(인용된 이름을 검색문서 `doc_metadata` name/champion 키와 대조), `mask_augment_win_rate_leak()`(승률 이중방어 2차선), `mask_opponent_nicknames()`(재사용 유틸, PGA-09/CHAT-10 예정). 재시도/폴백은 CHAT-05에서 이미 구현돼 중복 구현 안 함. 근거검증·승률마스킹이 완성 문장 단위로만 신뢰 가능해 `generate_answer_stream()`을 "실시간 토큰 그대로 전달"에서 "전체 버퍼링→후처리→공백 재분할 전송"으로 변경(PM 승인, 체감 지연 생기면 추후 개선). pytest 14건 신규(backend 149/149 통과) |
| 2026-08-04 | CHAT-07 완료(PM 승인) — `services/chat_links.py`로 CHAT-06의 인용 규칙(작은따옴표)을 재사용해 검색 문서에 실재하는 이름만 마크다운 링크로 치환. URL은 IA v1.2 그대로(조합 `/comps/{comp_id}` 개별 상세페이지, 아이템 빌드·증강체는 개별 상세페이지가 없어 목록 페이지 `/items/builds`·`/augments`). 검색 문서에 없는 이름(CHAT-06이 이미 경고 처리)은 링크를 만들 id 자체가 없어 원문 유지 — WBS "존재하지 않는 id 오탐 케이스"를 검증된 이름만 링크 대상으로 삼는 설계로 원천 차단. `chat_stream.py`에 CHAT-06 다음 단계로 배선. pytest 8건 신규(backend 157/157 통과) |
| 2026-08-04 | CHAT-09 완료(PM 승인) — 착수 전 콜드스타트 판별 임계값을 이번에 직접 확정(문서에 기준 없었음): 10,000ms 이상이면 콜드스타트로 간주(Render 실측 50초+와 웜 목표 p50 3초 사이 안전 지점, PRD 3-3). `services/chat_logging.py`로 `record_chat_log()` 구현(질의/의도/근거문서id/patch/답변/지연시간/콜드스타트 전체 필드 적재). `chat_stream.py`에서 Groq 스트리밍 시작~CHAT-07 링크삽입 완료까지 지연시간 측정 후 정상 답변 생성 흐름에서만 로깅(명확화/범위밖/패치없음 조기반환은 intent·patch_version이 없어 제외). pytest 6건 신규(backend 163/163 통과) |
| 2026-08-04 | CHAT-08 완료(PM 승인) — `services/chat_cache.py`로 `cache_key=sha256(정규화질문+patch_version)` 기반 Postgres 캐시(`chat_answer_cache`, upsert) 구현. `chat_stream.py`에서 대화이력 조회 시점을 의도분류보다 앞으로 옮겨 첫 턴 질문만 캐시 조회(hit 시 의도분류~Groq 호출 전부 스킵), 후속 턴은 캐시 아예 미조회. 패치 갱신은 cache_key 자체가 달라져 자연 무효화(이전 patch_version 행 물리 삭제는 DATA-15 담당). 캐시 hit 경로는 intent가 없어 chat_logs 미기록(이미 로깅된 답변 재사용 논리, CHAT-09 원칙과 일관). pytest 9건 신규(backend 172/172 통과) — **CHAT 그룹(01~09) 전체 완료** |
| 2026-08-04 | PM 결정 — FE-03(티어리스트 홈) TASK 설명의 "ISR 렌더링"이 FE-01의 정적 export(`output: "export"`) 결정과 모순됨을 재확인 후 해소: ISR은 요청마다 재생성 여부를 판단하는 서버가 필요해 정적 export와 원천적으로 양립 불가(Next.js가 빌드 에러로 막음), Cloudflare를 정적 assets 전용 Worker로 쓰는 SET-07 결정도 이미 확정돼 있어 ISR을 쓰려면 배포 아키텍처 자체를 재구성해야 함. 티어리스트 데이터는 어차피 배치가 하루 1회(DATA-14) 갱신하는 거라 ISR이든 빌드 시점 고정이든 "진짜 최신"을 보장 못 하므로, **정적 export로 페이지 셸만 빌드하고 브라우저에서 API를 직접 호출하는 클라이언트 사이드 fetch(CSR)** 방식으로 확정. `TFT_Hideout_WBS.xlsx` FE-03 TASK 설명(F69)을 "ISR 렌더링"→"정적 export(FE-01 결정) + 클라이언트 사이드 fetch(CSR)"로 갱신(그 외 변경 없음, diff 1셀만 확인). WBS 순서(그룹→하위영역→번호)상 다음은 프론트엔드 그룹이며, KPI모니터링보다 먼저 진행하기로 함 |
| 2026-08-04 | FE-03 완료(PM 승인) — 화면설계서 2.1 원문(컴포넌트/인터랙션/데이터바인딩/반응형 표) 직접 대조해 구현: PatchBadge/TierBadge/CompCard/CompGrid/FilterBar(데스크톱 드롭다운·모바일 바텀시트)/ChatWidgetToggle(전역 슬롯, 실제 UI는 FE-09). `lib/api-client.ts`·`api-config.ts` 신규(CLAUDE.md 10.1 공용 fetch 유틸, 정적 export라 API_BASE_URL은 빌드시점 상수). **착수 중 프로덕션 버그 2건 발견·수정**: (1) 백엔드에 CORS 설정이 전혀 없어 배포된 프론트엔드가 실제로는 데이터를 하나도 못 받아오던 문제(pytest/curl은 브라우저 CORS를 안 걸러서 못 봤음) — `CORSMiddleware` 추가 후 별도 커밋(b2e27dd)으로 즉시 배포. (2) 디자인 토큰에 없던 "OP" 티어를 op.gg 실 데이터로 발견해 레드 계열 임시 색상 추가, `design-tokens.md`에 기록(최종 확정 필요). Vitest+RTL 14건 신규(frontend 33/33 통과), Playwright로 배포된 프로덕션 백엔드 실데이터 기준 데스크톱/태블릿/모바일 3개 브레이크포인트 시각 검증 완료(콘솔 에러 0건) |
| 2026-08-04 | FE-04 완료(PM 승인) — 착수 중 API-03 프로덕션 버그 발견·수정(`recommended_items` 응답 스키마가 `dict`로 잘못 선언, 실제 배치는 `list`를 만들어 실 데이터로는 항상 500 에러였음 — `list[str]`로 수정). 챔피언/아이템/증강체를 이름 없이 ID로만 내려주던 설계 공백도 발견 — Riot API는 정적 데이터를 REST로 안 주고 Data Dragon 전용임을 확인하는 과정에서 DATA-06이 "연결 실패"로 결론냈던 공식 TFT Data Dragon이 실제로는 연결됨을 재확인(`docs/spike/tft-ddragon.md` 정정, 데이터 소스는 기존 Community Dragon 유지). 실 데이터로 JOIN 정합성 사전 검증(챔피언 86/86, 아이템 29/29 매칭) 후 API-03에 `name_kr`/`name_en`/`recommended_item_names` 추가. **조합 상세 URL을 IA v1.2 원문(`/comps/{comp_id}`)에서 쿼리스트링(`/comps?id=`)으로 전환**(PM 승인) — 정적 export는 빌드 시점 comp_id만 정적 페이지화 가능한데 comps가 패치마다 새 행으로 쌓여 매 패치마다 조합상세 링크가 깨지는 문제 발견, Cloudflare 배포 훅 자동 재빌드 대안도 검토했으나 FE-03이 거부한 ISR형 빌드시점 고정을 재도입하는 셈이라 판단해 쿼리스트링 채택. `glossary.md`·FE-03 `CompCard`·CHAT-07 `chat_links.py` 링크 동시 수정. 화면설계서 2.2 그대로 BackButton/CompOverview/ChampionList(캐리 accent-carry 2px 보더)/AugmentList/ItemBuildsCta(모바일 full-width) 구현. backend pytest 4건, frontend Vitest 15건 신규(backend 176/176·frontend 42/42 통과), 실제 배포 데이터로 브라우저 시각 검증 완료(콘솔 에러 0건) |
| 2026-08-05 | FE-05 완료(PM 승인) — 착수 중 이번 세션 최대 발견: DATA-08/DATA-10이 완료 처리됐지만 챔피언별 아이템 빌드 수집·저장 함수(`champion_item_build_rows`/`replace_champion_item_builds`, 개별 pytest는 통과 상태)가 실제 배치 오케스트레이션(`run_patch_batch.py`)에 한 번도 배선되지 않아 운영 `champion_item_builds`가 계속 비어있었음(실 API 호출로 발견) — `step_collect`/`step_normalize`에 배선 추가, 로컬 테스트 DB에 실제 op.gg 데이터로 배치를 돌려 54,289행·83명 중 63명 챔피언 확인. API-04도 FE-04의 API-03과 동일 패턴 버그(`item_combination` 응답이 `dict`로 잘못 선언, 실제는 `list[str]`) 발견·수정하며 `champion_name_kr/en`·`item_combination_names` 추가. 추가로 실데이터에서 챔피언 1명당 조합이 최대 1,735개까지 나오는 것을 확인해 SQL 윈도우 함수로 챔피언별 상위 10개(`TOP_BUILDS_PER_CHAMPION`)만 반환하도록 제한(PM 승인). 화면설계서 2.3 그대로 `/items/builds` 구현: ChampionSelect(Dropdown)/ItemComboList/BuildPriorityDetail(데스크톱 좌우분할·태블릿 상하스택·모바일 하단시트), 챔피언 필터 선택 시 URL 쿼리(`champion_id`,`patch`) 동기화. backend pytest 7건(전체 180/180)·frontend Vitest 3건(전체 45/45)·batch 기존 74/74 그대로 통과, `tsc`/eslint/prettier/`next build` 전부 통과. Playwright로 로컬 백엔드(실제 op.gg 데이터) 데스크톱/모바일 시각 검증(콘솔 에러 0건) — PM이 동일 로컬 DB에 연결한 dev 서버로 직접 브라우저 확인 후 승인. **운영 DB의 `champion_item_builds`는 아직 비어있어 배포 후 GitHub Actions 배치 수동 트리거(`workflow_dispatch`) 필요**(다음 세션 확인). 상세: `docs/verification/FE-05-작업결과.md` |
| 2026-08-05 | FE-05 운영 데이터 백필 완료 — PM이 `patch-detection.yml`을 `workflow_dispatch`로 수동 트리거했으나 21초 만에 성공 처리되고 `champion_item_builds`는 그대로 비어있었음. 원인: 이 워크플로우는 "패치 버전이 바뀌었을 때만" 전체 수집을 실행하는 구조(`run_patch_detection`)라, 운영 DB 현재 패치(17.8)와 op.gg 최신 패치(17.8)가 같아 `triggered=False`로 스킵됨 — 이번처럼 "기존에 없던 데이터를 오늘 처음 채우는" 스키마 변경 백필은 패치 감지 로직과 안 맞고, **다음 실제 패치 전까지는 재트리거해도 안 채워지는 구조적 공백**을 발견(추후 유사 케이스 재발 가능성 있어 기록). 패치 감지·승격 로직은 건드리지 않고 이미 검증된 `champion_item_build_rows()`/`replace_champion_item_builds()`를 재사용하는 1회성 백필 스크립트(레포 미커밋, 실행 후 삭제)를 PM 승인 하에 운영 DB에 직접 실행 — `champions`(읽기)·`champion_item_builds`(쓰기)만 사용, 다른 테이블 영향 없음. filled=63/empty=20/failed=0(로컬 검증과 동일 비율). `GET /catalog/items/builds` 630행 정상 확인, `/items/builds` 배포 페이지 200 — **FE-05 전체 종료**. 상세: `docs/verification/FE-05-작업결과.md` |
| 2026-08-05 | FE-06 완료(PM 승인) — 화면설계서 2.4 그대로 `/augments` 구현: TierFilterBar(FE-03 FilterBar와 동일 드롭다운/바텀시트 패턴)/AugmentGrid(CompGrid와 동일 반응형 그리드)/AugmentCard(이름/등급배지/설명/승률/관련조합링크). WBS 필수·정책 DoD(Legend 승률 미노출)를 백엔드 마스킹 + 프론트 `is_legend_related` 플래그 기반 이중 방어로 구현, Vitest 정책 테스트로 검증. 착수 중 API-05에 `related_comp_ids`(comp_augments 조인) 추가 발견·보완(comp_augments가 DATA-10 결정으로 아직 비어있어 FE-04 AugmentList와 동일하게 지금은 링크 항상 숨김). 증강체 등급(gold/silver/prism) 배지 색상은 design-tokens.md 미확정 항목이라 신규 채택(FE-03 OP 티어 색상과 동일 패턴). **착수 중 실데이터(357개) 모바일 검증으로 치명적 버그 발견·수정**: 일부 증강체 설명에 공백 없는 긴 원본 op.gg 텍스트(미해석 템플릿)가 그대로 있어 모바일 레이아웃 뷰포트가 옆으로 팽창(`window.innerWidth` 412→983px)하며 `position:fixed` 바텀시트가 화면 밖으로 밀려나던 문제 — `wrap-break-word` 추가로 수정, 재검증 확인(FE-03/FE-05는 페이지가 짧아 우연히 안 드러났을 가능성, TEST-09에서 재확인 권장). backend pytest 2건(전체 182/182)·frontend Vitest 9건(전체 54/54) 신규 통과, `tsc`/eslint/prettier/`next build` 전부 통과, Playwright로 로컬 백엔드(실제 op.gg 데이터) 데스크톱/모바일 시각 검증(콘솔 에러 0건). 상세: `docs/verification/FE-06-작업결과.md`. **PM 결정 — 증강체 설명에 남아있는 `<br>` 리터럴·미해석 `@TFTUnitProperty...@` 템플릿 정리를 별도 TASK로 분리**: `DATA-16`(증강체 설명 템플릿 플레이스홀더 정리, 권장, 선행TASK DATA-08) 신설 — `TFT_Hideout_WBS.xlsx`·본 문서 동시 반영, 전체 97개 TASK로 갱신(활성 94개) |
| 2026-08-05 | PM 결정 — FE-10(이미지 최적화)은 선행 TASK인 FE-07·FE-08이 SET-16(Riot API 키 재발급) 대기로 아직 안 끝났고, 최적화할 실제 이미지 자산도 없어(전부 placeholder) 착수 보류. WBS 순서상 FE-10보다 먼저이고 선행TASK(FE-01)만 충족하는 FE-11로 순서를 바꿔 진행하기로 함 |
| 2026-08-05 | FE-11 완료(PM 승인) — FE-03 `FilterBar`·FE-05 `ChampionSelect`·FE-06 `TierFilterBar`가 각자 구현하던 거의 동일한 Dropdown/BottomSheet 마크업을 공용 컴포넌트 2개(`FilterDropdown`, `ResponsiveFilter`)로 통합. FE-05 `ChampionSelect`는 `ResponsiveFilter`(바텀시트 포함)가 아니라 `FilterDropdown`만 재사용 — 화면설계서 2.3이 이 필터를 브레이크포인트 무관 항상 드롭다운으로 유지하도록 명시하고 이미 PM 승인된 FE-05 동작이라 바텀시트를 새로 추가하지 않음(의도적 설계 결정), "3개 화면 재사용" DoD는 `FilterDropdown`으로 충족. 리팩터 전후 렌더링 DOM을 완전히 동일하게 유지해 기존 FE-03/FE-05/FE-06 테스트 파일을 무수정으로 통과시켜 무회귀 확인. WBS 요구사항(Dropdown/BottomSheet 상태 동기화 테스트) `responsive-filter.test.tsx` 4건 신규, frontend 전체 58/58 통과. `tsc`/eslint/prettier/`next build` 전부 통과. Playwright로 배포된 프로덕션 데이터 기준 데스크톱/모바일 시각 검증(콘솔 에러 0건, 리팩터 전과 시각적으로 동일함을 확인). 상세: `docs/verification/FE-11-작업결과.md` |
| 2026-08-05 | DATA-16 완료(PM 승인) — `batch/normalize.py`에 `clean_augment_description()` 신규: `<br>` 리터럴 태그를 실제 개행(`\n`)으로, `@TFTUnitProperty...@` 미해석 수치 템플릿을 "(수치 정보 없음)"으로(op.gg가 실제 수치를 안 줘서 값 치환은 불가, DATA-05 스파이크 범위 확인), `<rules>` 등 나머지 HTML 유사 태그는 제거하도록 구현, `augment_rows()`에 적용. `AugmentCard`에 `whitespace-pre-line` 추가해 정리된 개행을 실제로 표시(FE-06 `wrap-break-word`와 함께 사용). pytest 6건(batch 전체 80/80)·Vitest 1건(frontend 전체 59/59) 신규 통과. 실제 op.gg 데이터 357개 전체로 검증 — 정리 전 `<`·`@` 잔여 패턴 99건(28%) → 정리 후 0건. **운영 DB 백필**: 다음 배치부터는 자동 적용되지만 기존 저장분은 그대로라, `augments` 테이블만 읽고 쓰는 1회성 백필 스크립트(레포 미커밋, op.gg 재호출 없음)를 PM 승인 하에 운영 DB에 직접 실행 — 357행 중 105행 변경, 운영 API로 재확인해 전체 잔여 패턴 0건 확인. 상세: `docs/verification/DATA-16-작업결과.md` |
| 2026-08-05 | API-02/FE-03 랭크 필터 롤백(PM 승인) — PM이 로컬 화면에서 티어리스트 "랭크"를 챌린저/마스터로 바꾸면 항상 빈 결과만 나오는 것을 발견해 문의. `tools/list`로 op.gg MCP `tft_list_meta_decks`의 `inputSchema`를 직접 재조회한 결과 파라미터가 전혀 없음(`{"type":"object","properties":{},"required":[]}`)을 확인 — 랭크 구간별 데이터를 얻을 방법 자체가 op.gg에 없음이 확정됨(`docs/spike/opgg-schema.md` 7번 신설). API-02 구현 당시(2026-08-03) "DATA-05 스파이크 완료 후 확정 필요"로 남겨뒀던 미확정 상태가 실제로는 **데이터 소스의 근본적 한계**였다고 정정. **PM 결정: 랭크 필터 기능 전체 제거**(향후 재추가 여지 없음) — `GET /catalog/tierlist`의 `rank` 쿼리·`ALLOWED_RANKS`·응답 `rank` 필드, `comps.rank_tier` 컬럼(마이그레이션 `2fdcd6f417ba`), 프론트 `FilterBar` 컴포넌트·`RANK_OPTIONS`/`RankValue` 타입을 전부 삭제. 티어리스트 페이지는 패치 배지만 표시하도록 단순화. backend pytest 180/180(rank 전용 2건 제거)·batch 80/80(영향 없음)·frontend Vitest 54/54(5건 제거) 전부 통과, `tsc`/eslint/prettier/`next build` 통과. FE-11(공용 필터 컴포넌트)의 "3개 화면 재사용" DoD는 이제 2개 화면(FE-05·FE-06)으로 줄었으나 공용 컴포넌트 자체의 결함은 아님(FE-03이 기능을 잃은 결과). 상세: `docs/verification/API-02-rollback-작업결과.md` |
| 2026-08-05 | FE-13 완료(PM 승인) — PM이 티어리스트 카드의 캐리 챔피언 점(●) placeholder를 실제 챔피언 이미지로 바꿀 수 있는지 문의. Community Dragon `squareIcon` 필드(.tex 게임 엔진 경로)를 `raw.communitydragon.org` CDN 규칙(전체 소문자+`.png`)으로 변환하면 실제 이미지를 받을 수 있음을 실호출로 확인(아칼리 등으로 200 응답 확인) — WBS FE-10(FE-07/08 완료 선행) 전체를 기다리지 않고 이 좁은 범위만 신규 TASK `FE-13`(권장, 선행TASK FE-03)으로 분리해 바로 진행(PM 승인). `champions.square_icon_url` 컬럼 추가, `batch/normalize.py`에 `cdragon_asset_url()` 신규(챔피언 아이콘 URL 변환), `GET /catalog/tierlist`의 `CompSummary.carry_champion_ids`(단순 id 배열)를 `carry_champions`(champion_id/name_kr/square_icon_url)로 확장, `CompCard`가 `next/image`로 실제 원형 아이콘(캐리 강조 보더 유지)을 렌더링하고 이미지 없으면 기존 placeholder 폴백. batch pytest 3건(전체 82/82)·backend pytest 2건(전체 182/182)·frontend Vitest 2건(전체 56/56) 신규 통과, `tsc`/eslint/prettier/`next build` 전부 통과. Playwright로 로컬 백엔드(실제 Community Dragon 데이터, 83명 중 78명 아이콘) 시각 검증(콘솔 에러 0건). **운영 데이터 백필 완료**(2026-08-05): 배포 확인(Render `carry_champions` 필드·Cloudflare 200) 후 Community Dragon 1회 호출로 `champions` 테이블만 UPDATE하는 1회성 스크립트 실행 — 83명 중 78명 업데이트(5명은 Community Dragon에 아이콘 자체 없어 매칭 없음, 기존 placeholder 유지). 배포된 사이트에서 Playwright로 원형 챔피언 아이콘 정상 렌더링 확인(콘솔 에러 0건). 상세: `docs/verification/FE-13-작업결과.md` |
| 2026-08-06 | FE-14 신규 등록·착수 — PM이 조합 상세 헥스 배치도(현재 is_carry 기반 휴리스틱)의 실제 좌표 반영 가능 여부를 문의. op.gg `tft_list_meta_decks` 응답을 직접 재조회한 결과 `units[].cell.{x,y}`(x:1~7, y:1~4, 4행×7열)에 실제 배치 좌표가 있음을 확인 — 이전 스파이크 요약(`docs/spike/opgg-schema.md`)에는 `stat` 필드 샘플만 남아있어 이 필드를 놓쳤던 것으로 확인, "op.gg·Riot 공식 API 어디에도 보드 좌표가 없다"던 이전 판단을 정정. PM이 신규 TASK로 분리해 진행을 요청해 `FE-14`(권장, 선행TASK FE-04)로 WBS.xlsx·본 문서에 등록하고 착수(상태: 진행중). |
| 2026-08-06 | FE-14 완료(PM 승인) — `comp_champions`에 `cell_x`/`cell_y` 컬럼 추가, `batch/normalize.py`의 `comp_champion_rows()`가 op.gg 응답 `unit.cell`에서 좌표를 추출(없으면 None), `GET /catalog/comps/{id}` 응답에 포함. `HexBoard`가 조합의 **모든** 챔피언에 좌표가 있으면 CSS Grid(7열×4행, 짝수 행 반 칸 오프셋)로 실좌표 배치도를 그리고, 하나라도 없으면(구 데이터) 기존 `is_carry` 휴리스틱으로 전체 폴백 — 이후 PM 요청으로 빈 칸도 옅은 회색 헥스 테두리로 그려 몇 번째 줄·칸인지 배경 격자선을 추가. 좌표 y/오프셋 방향은 op.gg 응답에 방향 메타데이터가 없어 임의로 정함(상대적 배치는 실데이터 그대로, 좌우·상하 반전 가능성 있음, PM에 고지). backend pytest 185/185(신규 2건)·batch pytest 83/83(신규 1건)·frontend Vitest 73/73(신규 4건) 전부 통과, `tsc`/eslint/prettier 통과. **운영 데이터 백필**: 마이그레이션을 실제 Supabase 개발 DB에 적용 후 op.gg `tft_list_meta_decks` 1회 재호출로 `comp_champions.cell_x/cell_y`만 UPDATE하는 1회성 스크립트 실행(레포 미커밋, 다른 테이블 미변경) — 10개 조합 중 8개(69명) 실좌표 반영, 2개는 op.gg 응답에서 해당 조합을 못 찾아 휴리스틱 폴백 유지(메타 변동 추정). 같은 세션에서 챔피언/아이템 아이콘(`items.square_icon_url`)·증강체 대표 이미지(`augments.image_url`)·완성 아이템 재료 팝오버(신규 `GET /catalog/items`)·아이템 빌드 챔피언 자동완성·증강체 티어 필터 제거 등 다수의 PM 요청 UI 개선을 함께 반영(각각 WBS 미등록 범위, 상세는 각 기능 커밋 메시지 참고). 상세: `docs/verification/FE-14-작업결과.md` |
| 2026-08-06 | FE-09 완료(PM 승인) — 화면설계서 2.7 챗봇 위젯(전역 컴포넌트) 구현. `ChatWidget`이 `layout.tsx`에 전역 삽입되어 전 페이지 노출, 데스크톱/태블릿은 우하단 플로팅 패널(360×505, radius 10), 모바일은 하단 고정 바 → 탭 시 전체화면 바텀시트로 전환(FE-01 placeholder는 모바일에서도 작은 플로팅이던 버그를 함께 수정). `chat-stream.ts`가 API-09/CHAT-05의 SSE(`data: <token>` 반복 + `event: done`)를 파싱해 토큰 단위로 렌더링, CHAT-07이 만든 `[이름](url)` 링크를 클릭 가능한 상세 페이지 링크로 렌더링하고 클릭 시 `link_click_events`(KPI 전환율 계측)에 로깅. 후속질문칩은 백엔드에 맥락별 생성 로직이 없어 의도 4분류 대표 고정 질문 4개를 빈 대화 상태에서만 노출하는 가정으로 구현(PM 확인 필요 항목으로 별도 보고, 아래 CHAT-11 참고). Vitest+RTL 4건(WBS 요구사항: collapsed/expanded 전환·모바일 바텀시트 전환·mock SSE 렌더링·링크 렌더링) 신규, frontend 전체 77/77 통과, eslint/prettier/tsc/`next build` 전부 통과. Playwright로 데스크톱/모바일 스크린샷 확인 및 **배포된 실제 백엔드로 end-to-end 메시지 전송**(Groq 실답변 스트리밍·패치 버전 명시·링크 렌더링 확인, 콘솔 에러 0건). 상세: `docs/verification/FE-09-작업결과.md` |
| 2026-08-06 | CHAT-11 신규 등록 — FE-09 PM 확인 과정에서 후속질문칩을 맥락 기반으로 동적 생성할 수 있는지 PM이 문의. 현재 CHAT-01~09 파이프라인 어디에도 후속 질문 생성 로직이 없어 규칙 기반(intent+검색문서 템플릿) vs LLM 기반(Groq 추가 호출) 두 방식과 트레이드오프(비용/지연 vs 자연스러움)를 보고, PM이 **LLM 기반 채택**을 결정. `CHAT-11`(권장, 선행TASK CHAT-05·FE-09)로 WBS.xlsx·본 문서에 신규 등록(상태: 대기) — 프롬프트 설계, SSE `event: followups` 신규 이벤트 추가, 무료 티어 레이트리밋/지연 영향 검토를 DoD에 포함. |
